# 图片组的最后空位平时不显示内容，但是如果鼠标移动到这个位置，则显示一个上下排列的插入和截图按钮，可以点击来在这里插入图片
# 截图的按钮改成一个剪刀的图标
# 图片组如果前面部分没有图片，后面被放在了下一页那么直接把整个框移动到下一页，前一页后面留空就好
# 组件的四个边缘，鼠标hook上去的时候都可以显示双向箭头，可以用来对组件进行缩放
# 实现一个自动布局的逻辑，通过改动组件的大小和位置（按照从从上到下从左到右进行排序），组件可以放在同一行（如果宽度都不大），自动计算并进行布局调整来填充页面
# 组件的名字可以编辑，但是组件名的文本框不要占据整个横条，应该只和文本宽度一致，其他部分属于卡片边框，点击卡片边框可以拖动卡片，除了顶端之外，左右和下边都需要有卡片边框，都可以进行拖动，他们的宽度和顶端一致
# 参考图组件，默认自带一个文本说明框，里面默认文本是“参考图说明”，右侧可以显示一个 "隐藏" 勾选框，表示是否显示文本，会影响pdf导出效果
# 图片组默认不显示图片的说明，每个图片单独决定是否有文字说明，鼠标hook到图片上也显示说明框，可以编辑，编辑之后显示在图片的下方，缩小图片的高度来放文字，最多用1/3的高度来放文字，如果文字太多则缩小文字字号，文字字号最小是5号，如果已经放不下，则自动调整整体图片组的尺寸（高度）
# 截图是否可以用js原生的库来实现？调起windows原生的截图工具似乎比较重
# 图片尺寸这块，需要显示当前的高度 pixel，便于各个图片组进行统一设置
# 图片组里面的图片，hook到图片四周之后可以通过拖动来改变图片的尺寸，然后缩放到这个图片组的高度，保持宽高比，记录经过缩放的图片，hook上去之后在左下角显示一个按钮恢复原图，点击之后恢复原图的尺寸
# 