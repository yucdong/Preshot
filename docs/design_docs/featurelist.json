{
  "project": "Preshot",
  "description": "Photographic preparation desktop tool",
  "features": [
    {
      "name": "Workspace Setup",
      "feature_descriptions": [
        "app有一个自己的metadata存储，对于windows可以放在用户的appdata目录",
        "metadata存储记录了用户的信息，比如projects",
        "用户的projects是一个一个的文件夹，里面包含了每个拍摄项目相关的文件",
        "项目打开界面将会显示所有的projects，显示前三个，并且用一个滑块拖动查看后面的项目，项目用一张图片（如果项目中有图片）展示，如果没有图片，则显示项目名字",
        "点击项目后可以打开这个主界面",
        "页面的菜单栏有文件下拉菜单，可以选择新建、关闭、打开，打开新窗口等，打开新项目就会让你重新选择一个项目所在的目录",
        "每个项目文件夹下面有一个.preshot的文件表示这是一个preshot项目，打开项目必须检索到这个文件才能支持打开",
        "打开app后直接进入最近编辑的项目（无需先显示所有项目的开始页），最左侧栏显示按最后编辑时间排序（最新在最上）的项目列表，点击列表中的项目即可切换项目"
      ],
      "status": "completed",
      "decisions": [
        "Use TypeScript domain use cases with narrow Tauri adapters and Rust filesystem commands.",
        "Store workspace metadata in AppData through Tauri Store.",
        "Use a versioned JSON .preshot manifest with project identity and timestamps.",
        "Create projects by selecting a parent directory and entering a project name.",
        "Use an editorial horizontal gallery showing three recent projects at a time.",
        "Resolve covers from manifest coverImage, then the first supported root image.",
        "Keep invalid projects visible and require matching project IDs for relocation.",
        "Use a native Tauri File menu; new windows start the same auto-open flow.",
        "Auto-open the most recently edited available project on startup into a left project-switcher rail (sorted by updatedAt desc); show the launcher only when no available project exists, and route unavailable-project clicks there to recover them.",
        "Route pnpm Tauri commands through a Windows wrapper that discovers Cargo in the standard rustup directory when the parent VS Code PATH is stale."
      ],
      "progress": {
        "completedAt": "2026-07-28",
        "completed": [
          "Enabled Tauri Store and Dialog dependencies, permissions, and CSP.",
          "Implemented typed workspace registry rules and serialized workspace use cases.",
          "Implemented guarded Rust project creation, inspection, cover resolution, and token-authorized rollback.",
          "Implemented Store, Dialog, native command, menu event, and structured logging adapters.",
          "Implemented the accessible three-card recent-project launcher and new-project dialog.",
          "Integrated WorkspaceProvider, explicit production/E2E dependencies, native File menu, and multi-window launcher behavior.",
          "Hardened the native menu callback so guarded-action failures never surface as unhandled promise rejections.",
          "Made the in-memory workspace adapter fail closed in production builds so it stays end-to-end only.",
          "Prevented a busy create submission from closing the dialog without creating a project.",
          "Added the memory-adapter E2E browser flow and documented workspace architecture, testing, and reliability.",
          "2026-08-08: added scripts/tauri.ps1 so pnpm tauri:dev and pnpm tauri:build work when Cargo exists under %USERPROFILE%\\.cargo\\bin but the current VS Code PATH has not refreshed."
        ],
        "lastVerified": [
          "114 frontend tests passed (Vitest).",
          "21 Rust tests passed.",
          "Playwright browser smoke test and init.ps1 harness test passed.",
          "ESLint, TypeScript typecheck, and cargo fmt --check passed.",
          "pnpm build and pnpm tauri:build succeeded, producing MSI and NSIS bundles."
        ],
        "remaining": []
      }
    },
    {
      "name": "基础方案编辑",
      "feature_descriptions": [
        "project打开后，首先最重要的一栏是方案",
        "点击方案，右侧会显示几个tab，分别是摄影计划、参考样图",
        "参考样图可以添加多组，每组可以编辑每行显示的图片数量，然后按照这个去排版",
        "显示的时候将每张图压缩到1:1放入一行，鼠标点击之后，显示这个图片的完整内容",
        "摄影计划和参考样图显示在同一个面板中（无需切换tab），所见即所得",
        "摄影计划与每组描述支持富文本编辑（HTML），基于 TipTap，向后兼容旧的纯文本",
        "每组参考样图带有一个可编辑的描述文本框，标题与描述使用黑色高对比字体",
        "project状态每隔5秒自动保存：检测到变化才写入.preshot，无变化则跳过",
        "面板顶部显示保存状态（保存中/未保存/已保存），支持 Ctrl/Cmd+S 立即保存",
        "将方案与参考样图排版导出为 A4 PDF；图片按每行数量以正方形站位等比留白填充",
        "导出 PDF 时弹出保存位置对话框，默认保存到项目目录下的 output.pdf",
        "主界面采用三栏布局：左侧项目栏约15%、中间方案区约60%、右侧约25%为预留的对话/agent面板（底部含输入框占位）",
        "参考样图缩略图限定为固定尺寸（最大约160px方格），避免占比过大",
        "富文本编辑器提供工具栏：加粗、斜体、下划线、删除线、标题、列表、字号、颜色、链接，所见即所得",
        "导出的 PDF 中每张图片占据固定正方形站位并带浅灰色外框标示范围，图片按尺寸在框内留白；文本渲染支持下划线、删除线、颜色与字号",
        "富文本编辑器改用 BlockNote，提供 Notion 风格的块编辑（斜杠菜单、拖拽块、清单/表格/代码块），方案与每组描述统一；持久化仍为 HTML，向后兼容旧内容",
        "参考样图支持鼠标拖拽重新排序：可在组内调整顺序，也可拖到其它组；松开在非法位置则取消，移动后原组图片自动回填空位",
        "拖动参考样图时实时预览松手后的排版效果：其余图片让位显示插入位置（可插到组的最前、中间、结尾，也可拖入空的组），所见即所得；仅移动单张图并回填，不做换位"
      ],
      "status": "completed",
      "decisions": [
        "Implement Photography Plan and per-group description rich-text editing with a shared TipTap editor.",
        "Store plan data inside the versioned .preshot manifest as an optional plan field (schemaVersion 1).",
        "Copy imported images into a references/ subfolder with new sequential names while preserving the original files; accept only jpg/png up to 16MB.",
        "Render reference images as on-demand base64 data URLs via narrow Rust commands, not the asset protocol.",
        "Display images as 1:1 squares in configurable per-row grids; clicking expands a lightbox.",
        "Reuse the workspace layering: pure domain PlanService use cases, Tauri adapters, and narrow Rust commands.",
        "Merge Photography Plan and Reference Images into one scrollable panel (no tabs) for a WYSIWYG plan view.",
        "Give each reference group an editable description (persisted on blur) with high-contrast black title and description text; ReferenceGroup.description defaults to empty for older manifests.",
        "Store TipTap schema-safe HTML in .preshot, keep edits in memory with 5-second auto-save flushing, and stay backward-compatible with legacy plain-text content.",
        "Auto-save the plan every 5s from ProjectPlanProvider with JSON change detection (skip writes when unchanged); pure-metadata edits defer to the loop while file side-effect ops (import/remove/delete-group) save immediately, plus an unmount flush. PlanService.savePlan is the single persistence use case.",
        "Use native pdf-lib + fontkit export with bundled Noto Sans SC, square contain-fit image slots, and saving via the dialog + save_pdf command.",
        "Default the PDF export save dialog to <project>/output.pdf.",
        "Adopt a three-column app shell (15% projects / 60% plan / 25% Assistant preview) with a disabled bottom input reserved for a future agent.",
        "Cap reference thumbnails at ~160px squares (grid minmax(0,160px), left-aligned) so the gallery stays bounded.",
        "Extend the shared rich-text toolbar with underline, strikethrough, font-size (custom TextStyle FontSize extension), and color, rendered WYSIWYG via .ProseMirror styles.",
        "Render underline/strikethrough/color/font-size runs in the exported PDF and draw a light-gray frame around every square image slot.",
        "Replace TipTap with BlockNote (0.52, Mantine) in both the plan body and group descriptions; keep HTML persistence via tryParseHTMLToBlocks/blocksToHTMLLossy (no migration); adopt native BlockNote formatting (drop font-size/hex); PDF renders checklists as bullets, code as preformatted text, tables flattened; editor coverage shifts to e2e with mocked component tests.",
        "Add reference-image drag-and-drop with @dnd-kit (confined to src/features/plan): a pure moveImage reducer + non-persisting use case (deferred 5s auto-save, no file I/O) and a pure resolveImageMove helper; whole-tile drag with a pointer activation distance so clicks still open the lightbox; invalid drops cancel; drop logic is unit-tested (jsdom cannot drive real drags).",
        "Drag preview shows the post-drop layout live by reusing the moveImage reducer on an optimistic copy (preview === commit); a pure computeDropTarget/dropTargetFromEvent maps the dnd-kit event to {toGroupId,toIndex} for front/middle/end, cross-group, and empty-group insertion; v1 resolveImageMove/handleImageDragEnd removed."
      ],
      "progress": {
        "designApprovedAt": "2026-07-29",
        "completedAt": "2026-07-29",
        "designSpec": "docs/superpowers/specs/2026-07-29-basic-plan-editing-design.md",
        "implementationPlan": "docs/superpowers/plans/2026-07-29-basic-plan-editing.md",
        "completed": [
          "Brainstormed intent and scope, approved the design, and committed the spec.",
          "Wrote the task-by-task TDD implementation plan (13 tasks).",
          "Implemented the pure plan domain: models, reducers, and a serialized PlanService.",
          "Added narrow Rust commands (save/read plan, import/load/remove reference image) storing plan in the .preshot manifest and images under references/.",
          "Added Tauri and in-memory plan adapters plus fail-closed dependency composition.",
          "Built the Plan panel, Reference Images gallery, and lightbox UI, wired into the opened project.",
          "Added the E2E browser flow and documented the feature in ARCHITECTURE and TESTING.",
          "Merged Photography Plan and Reference Images into one tab-free scrollable panel, upgraded the Photography Plan body and per-group descriptions to shared rich-text editing, and kept title/description contrast black.",
          "Added a 5s change-detected auto-save in ProjectPlanProvider (metadata edits deferred, file ops immediate, unmount flush) backed by a new PlanService.savePlan use case.",
          "Added a live SaveStatus indicator (saving/unsaved/saved) in the PlanPanel header and a Ctrl/Cmd+S shortcut that flushes immediately.",
          "Added A4 PDF export (pure domain export document + geometry, pdf-lib/fontkit with bundled Noto Sans SC, dialog/save_pdf save path) and covered it with browser smoke + docs.",
          "Optimized the plan workflow: PDF export defaults to <project>/output.pdf via the save dialog; the main window uses a 15/60/25 three-column shell with an Assistant preview panel and a disabled bottom input; reference thumbnails are capped at ~160px squares; the rich-text toolbar gained underline/strikethrough/font-size/color WYSIWYG controls; and the PDF renders those marks plus a light-gray frame around each square image slot.",
          "Migrated the shared rich-text editor from TipTap to BlockNote (Notion-style block editing) with a compact description variant, HTML-boundary conversion, jsdom shims + mocked component tests, an e2e editing smoke, and PDF handling for BlockNote block types.",
          "Fixed a hydration echo where BlockNote emitted onChange on programmatic load, which flipped a project to unsaved on open and rewrote .preshot into normalized HTML; the editor now suppresses the load echo so existing content loads unchanged.",
          "Added drag-and-drop reordering of reference images within and across groups (dnd-kit) backed by a pure moveImage reducer/use case and a pure drop resolver, with deferred auto-save persistence.",
          "Kept image tiles keyboard-openable by using a pointer-only sensor (dragging is mouse/pointer only) and used closestCorners collision for precise slot insertion.",
          "Added WYSIWYG live-reflow preview for reference-image dragging (tiles reflow to the insertion point across groups incl. empty), reusing the moveImage reducer so the preview matches the committed move; drop-target math is pure and unit-tested with an e2e drag.",
          "Fixed an infinite render loop when dragging into an empty group: onDragOver now only updates the preview for a valid, changed target and never reverts mid-drag (dnd-kit re-fires onDragOver on measuring changes).",
          "Fixed reference-image drops silently not committing into empty/other groups: onDragEnd now commits the release-time target and falls back to the last previewed target when the pointer is released over the dragged tile's own previewed slot (over === activeId → null), so both within-group reordering and cross/empty-group drops commit.",
          "Made dropping onto an empty group reliable by switching dnd-kit collision detection to pointer-within (fallback closest-corners): the droppable the pointer is inside — including an empty group's '+' slot — is targeted directly, instead of a distance heuristic that could favour a larger neighbouring group over a small empty container.",
          "Made dragging more sensitive: collision detection now uses rect-intersection (the image tile the dragged image overlaps most wins, group containers skipped so tile positioning beats append; pointer-within/closest-corners remain the fallback for empty groups and gaps), and same-group drop math uses array-move semantics so the active image takes the over tile's slot as soon as it overlaps it most — no longer requiring the pointer to travel past the tile's centre. Empty-group drops keep working via the fallback.",
          "Fixed the left project rail: the app shell is now a fixed-height flex column (h-screen + overflow-hidden) so only the middle plan panel scrolls internally and the New/Open project actions stay pinned in the sidebar instead of scrolling away with the page. The projects list is capped at ~15 rows (max-h-[38rem] + overflow-y-auto, shrinks on short viewports) with a scrollbar beyond 15, and the actions are anchored to the bottom via a flex spacer.",
          "Fixed a regression from the fixed-height shell where the middle plan panel could not scroll (groups below the fold were clipped): completed the flex min-h-0 chain by adding min-h-0 to the Workspace <main>, so PlanPanel's overflow-y-auto is height-bounded and scrolls. Covered by an e2e that asserts the panel is scrollable and reveals the last group."
        ],
        "lastVerified": [
          "204 frontend tests passed (Vitest).",
          "30 Rust tests passed.",
          "10 Playwright e2e tests passed.",
          "ESLint and TypeScript typecheck passed.",
          "pnpm build succeeded."
        ],
        "remaining": []
      }
    },
    {
      "name": "Internationalization Foundation",
      "feature_descriptions": [
        "Application defaults to Chinese (zh) language across the entire UI",
        "Implemented react-i18next with typed translation keys for type-safe localization",
        "All user-facing strings use localized translation keys (shell, agent, plan, reference, workspace, dialogs, errors)",
        "Chinese demo seed content: photography plan '日落大片' and reference group '造型参考'",
        "BlockNote editor configured with Chinese dictionary for block menu and UI elements",
        "Generic error banners display Chinese messages",
        "Browser demo seed and e2e tests updated to Chinese strings",
        "Full Chinese UI covers: project launcher, workspace navigation, plan editing, reference images, save status, PDF export, BlockNote editor, lightbox, dialogs, and error boundaries"
      ],
      "status": "completed",
      "decisions": [
        "Use react-i18next (v15.3) with typed keys via i18next module augmentation of CustomTypeOptions with resources: { translation: typeof zh } for compile-time safety",
        "Store Chinese translations in src/shared/i18n/locales/zh.ts as a const object for type inference",
        "Configure i18next with lng: 'zh', fallbackLng: 'zh' so the app defaults to Chinese without browser language detection",
        "Localize BlockNote editor using the built-in @blocknote/core/locales zh dictionary export imported directly for type safety",
        "Use t() in all React components for UI strings; app chrome, reference labels, save status, workspace launcher, and error messages all go through i18n",
        "Update browser demo seed to Chinese (static data, not i18n keys) so the seeded project displays Chinese content on first launch",
        "Update all e2e Playwright queries to Chinese strings to match the localized UI (role/text queries for buttons, save status, group names, etc.)",
        "Keep structural aria-labels (e.g., PlanPanel outer 'Plan') unlocalised when they are not user-visible"
      ],
      "progress": {
        "completedAt": "2026-08-03",
        "completed": [
          "Installed react-i18next (v15.3) and i18next; configured i18next with lng: 'zh', fallbackLng: 'zh', and typed resources",
          "Built a typed zh translation dictionary covering all UI surfaces: shell (projects, tagline, actions), agent (preview, input), plan (export, tab labels, block editor), reference (add/remove/open image, groups), save status (saving/unsaved/saved), workspace (launcher intro, cards, dialogs), errors (workspace/plan generic messages, error boundary)",
          "Wrapped App with I18nextProvider and tested i18n initialization",
          "Localized AppShell (tagline, projects nav), AgentPanel (title, preview, input), PlanPanel (export button, save status), PhotographyPlanTab (section label), ReferenceImagesTab (heading, add group button), ReferenceGroup (title/description labels, delete/add image), SortableImageTile (open/remove image aria-labels), ReferenceImageLightbox (close button), SaveStatus (statuses), WorkspaceLauncher (intro, actions, empty state), ProjectRail (recent projects, previous/next hints), ProjectCard (open/relocate/remove actions, unavailable hint), CreateProjectDialog (eyebrow, title, labels, cancel/create), GenericErrorBanner (workspace/plan messages), and AppErrorBoundary (title, body)",
          "Configured BlockNote editor with the built-in @blocknote/core/locales zh dictionary imported directly via import { zh } from '@blocknote/core/locales' and passed to useCreateBlockNote({ dictionary: zh }) for type-safe Notion-style block editing in Chinese",
          "Updated browser demo seed (browserPlan.ts SEEDED_PLAN) to Chinese: '日落大片' photography plan, '造型参考' reference group title/description; updated browserWorkspace.ts demo project name to '编辑大片示例'; updated corresponding test assertions",
          "Updated all e2e Playwright queries to Chinese: plan.spec.ts ('参考分组：造型参考', '打开参考图 X', '添加参考图', '添加参考分组', '导出 PDF', '正在导出…', '关闭图片', '已保存所有更改', '有未保存的更改', '摄影计划'), layout.spec.ts ('新建项目', '打开项目', '添加参考分组', '参考分组：新建分组', '添加参考图', and PlanPanel structural 'Plan' kept as-is), workspace.spec.ts ('项目', '打开项目 编辑大片示例', '添加参考分组')",
          "Ran full validation matrix: pnpm typecheck (clean), pnpm lint (clean), pnpm test (207 tests passed), pnpm test:e2e (10 e2e tests passed), pnpm build (succeeded)"
        ],
        "lastVerified": {
          "date": "2026-08-03",
          "typecheck": "clean",
          "lint": "clean",
          "unitTests": "207 passed",
          "e2eTests": "10 passed",
          "build": "succeeded"
        },
        "remaining": []
      }
    },
    {
      "name": "Canvas Component System",
      "feature_descriptions": [
        "Plan editing migrated from fixed v1 tabs to a flexible v2 canvas component system",
        "A4-based flow layout: components stack vertically, auto-paginate with WYSIWYG page breaks",
        "Insert menu: add Plan or Reference components via button; drag handle to reorder components",
        "Plan component: BlockNote rich-text editor, configurable height, seeds with Chinese default template (拍摄时间/拍摄地点/道具和服装/器材)",
        "Reference component: configurable title, optional description, image import, drag-to-reorder images within group, and independent per-image caption editors in screen and PDF",
        "Resize handles: adjust component widths (1/2, 2/3, 1) and heights interactively",
        "PDF export: render canvas to multi-page A4 PDF preserving exact WYSIWYG layout with font subsetting (small file sizes), image slots fit gutter-inset content box",
        "Persistent schemaVersion 2 format in .preshot manifest (v1 removed)",
        "Auto-save: 5s debounced save with save-status indicator, Ctrl/Cmd+S shortcut",
        "Add-description affordance: button reveals the description editor when description is empty"
      ],
      "status": "completed",
      "decisions": [
        "Adopt a v2 schemaVersion 2 plan model (ProjectPlan.components array) with Plan and Reference component types, each with id, widthFraction (1 | 1/2 | 2/3), height, and type-specific fields",
        "Build pure domain canvas engine: A4 pagination, flow layout (single column, no CSS Grid complexity), geometry (containSize, reflowPages), and WYSIWYG insert/move/resize reducers",
        "Implement canvas service: loadPlan (migrate v1 → v2), insertComponent, moveComponent, resizeComponent, updatePlanHtml, setReference* mutators, importImage (files reused across versions)",
        "Build ProjectCanvasProvider: manages canvas state, debounced auto-save, PDF export orchestration, picker/image loading wiring",
        "PlanCanvas UI: renders A4 pages + page-break indicators; each component gets a chrome (drag handle, insert-below menu, delete button, resize handles)",
        "Plan component view: BlockNote editor (zh locale), height resize",
        "Reference component view: title input, columns select, optional description editor, GroupImageGrid (reused from v1), height resize",
        "Insert component menu: dropdown (Plan / Reference), insert below target or at end",
        "Canvas drop geometry: @dnd-kit sortable components (pointer sensor, closestCenter collision, vertical list), drop indicator lines",
        "Canvas PDF exporter: reuse htmlToBlocks tokenizer + Noto Sans SC fonts, render each component with A4 pagination, square image slots, WYSIWYG to pdf-lib output",
        "Remove v1: delete ProjectPlanProvider, PlanPanel, PhotographyPlanTab, ReferenceImagesTab, dropTarget, v1 domain (models/plan/service/pdf), v1 pdfLibExporter; keep reused GroupImageGrid/SortableImageTile/ReferenceImageLightbox/htmlToBlocks/fonts; remove v1 ports/tauriPlan/browserPlan exports",
        "Wire canvas as default: planDependencies.ts returns createCanvasPlanDependencies (v2); Workspace.tsx renders ProjectCanvasProvider",
        "Phase C: plan default template (i18n content.planTemplate) seeded on insert; independent per-image caption editors wired to ReferenceImage.caption; PDF font subsetting enabled for smaller file sizes (~16 MB → <1 MB)",
        "Phase D: within-group image drag-reorder (@dnd-kit nested droppables: image tiles are sortable, GroupImageGrid is an image group droppable, PlanCanvas has a parallel image drag path with optimistic moveImage preview, ProjectCanvasProvider.handleMoveImage applies it client-side, 5s auto-save persists); PDF image slots now fit the gutter-inset content box (previously overflowed ~6 pt)"
      ],
      "progress": {
        "completedAt": "2026-08-03",
        "designSpec": "docs/superpowers/specs/2026-08-03-canvas-component-system-design.md",
        "implementationPlan": [
          "docs/superpowers/plans/2026-08-03-canvas-phase-a-domain-foundation.md",
          "docs/superpowers/plans/2026-08-03-canvas-phase-b-wiring-pdf.md"
        ],
        "completed": [
          "Designed canvas component system: v2 model, A4 flow engine, WYSIWYG insert/move/resize, component-level PDF export",
          "Phase A: implemented v2 domain (canvas/models, canvas/geometry, canvas/engine pagination/layout, canvas/plan reducers, canvas/service with v1 migration, canvas/pdf/exportDocument, canvas/dropTarget, canvas/canvasDropGeometry)",
          "Phase B task 1-3: built ProjectCanvasProvider (state/save/export wiring), PlanCanvas (A4 pages + chrome), PlanComponentView (BlockNote editor)",
          "Phase B task 4-5: built ReferenceComponentView (reused GroupImageGrid), InsertComponentMenu, useComponentResize hook",
          "Phase B task 6: built canvasPdfExporter (htmlToBlocks-based, multi-page A4 output)",
          "Phase B task 7: wired canvas as the default plan view in Workspace.tsx + planDependencies.ts",
          "Phase B task 8 (final): removed v1 model/UI/PDF (ProjectPlanProvider, PlanPanel, PhotographyPlanTab, ReferenceImagesTab, dropTarget, domain/plan/{models,plan,service,pdf/*}, pdfLibExporter); removed v1 exports from tauriPlan/browserPlan; kept reused GroupImageGrid/SortableImageTile/ReferenceImageLightbox (structural interface), htmlToBlocks, font assets; moved PdfSaveTarget to canvas/ports",
          "Phase C: delivered plan default template (content.planTemplate i18n key) and independent per-image caption editors (ReferenceImage.caption, screen + PDF); PDF font subsetting reduced file size from ~16 MB to <1 MB",
          "Phase D task 1: fixed PDF image slots to fit gutter-inset content box (previously overflowed ~6 pt horizontally)",
          "Phase D task 2-3: implemented within-group image drag-reorder (pure imageDropTarget helper, imageInsertAfterFromRects geometry, nested dnd-kit droppables, optimistic moveImage preview + commit via ProjectCanvasProvider.handleMoveImage, 5s auto-save persists)",
          "Phase D task 4 (final): added e2e for image reorder (mirrors component reorder test, asserts committed order change via data-image-id), updated featurelist, ran full validation matrix"
        ],
        "lastVerified": {
          "date": "2026-08-04",
          "typecheck": "clean",
          "lint": "clean",
          "unitTests": "283 tests passed",
          "e2eTests": "15 tests passed (canvas insert/reorder/resize/image/PDF/plan-template/captions/image-reorder-testability, layout scroll, workspace auto-open)",
          "rustTests": "29 tests passed",
          "build": "succeeded"
        },
        "remaining": []
      }
    },
    {
      "decisions": [
        "Replace fractional presets with continuous drag: widthFraction is a number 0.4–1.0 clamped and quantized to 0.01 step; double-click handles snap to 0.5/0.67/1.0; remove WidthResizeMenu dropdown entirely",
        "Unified SPACING constant = 24px for all canvas margins, gaps, and handle sizes (DRY, visual rhythm)",
        "Drag handle extends to the entire top bar: move handle becomes invisible (w-0, opacity-0, focusable for a11y); the top bar itself is draggable; low-emphasis gray type label added for component identification",
        "Four-edge resizing: add left/top/bottom handles to ComponentFrame; ResizeDirection enum ('width' | 'height' | 'both' | 'left' | 'top'); useComponentResize hook recognizes all five directions; resizeComponent reducer clamps width 0.4–1.0 and height 120–1000",
        "Delete confirmation: wrap ComponentFrame delete in AlertDialog with 删除/取消 actions; deletion only commits on 删除",
        "Insert at top (array.unshift): insertComponent reducer prepends to components array at index 0 instead of pushing to end; WYSIWYG matches user expectation for stacking order",
        "Seed plan + reference on project creation: EMPTY_PLAN_V3 factory includes one plan component (seeded with content.planTemplate) and one reference component (title '新建分组')",
        "Reference image aspect ratio: SortableImageTile renders img at natural aspect ratio (height: auto, max-h: imageHeight) instead of 1:1; ReferenceComponent.imageHeight controls the max-height (default 160); ReferenceComponentView adds a stepper (±20, range 60–300) to adjust imageHeight per component",
        "Multi-select images: Tauri dialog filter multiple: true; import_images command loops and maps the file array; createReferenceImage generates sequential IDs for batch import",
        "PDF export folder reveal: export_canvas_to_pdf returns the saved PDF path; a new export_folder Rust command invokes platform-specific folder-reveal (Windows: explorer /select, macOS: open -R, Linux: xdg-open parent); ProjectCanvasProvider calls it after save completes",
        "SchemaVersion 3 migration: CURRENT_SCHEMA_VERSION = 3; migrateV2ToV3 converts widthFraction strings to numbers (map {1/2→0.5, 2/3→0.67, 1→1}), adds imageHeight: 160 to reference components; loadPlan chains migrateV1ToV2 → migrateV2ToV3"
      ],
      "feature_descriptions": [
        "Continuous component width: drag any border (left/right) to adjust width fluidly; widthFraction persisted as number (0.4–1.0, step 0.01); double-click handles snap to presets (1/2, 2/3, 1)",
        "Unified spacing constant: SPACING = 24px for top/left/right margins, inter-component gaps, and handle sizes",
        "Move components by whole top bar: draggable area is the entire top bar (not just the drag handle icon); low-emphasis type label (摄影计划/参考图组) helps identify components",
        "Four-edge + corner resize: drag left/right edges for width, top/bottom for height, bottom-right corner for both; height 120–1000px",
        "Delete confirmation dialog: clicking × shows AlertDialog ('确定删除该组件？') before removing the component",
        "Insert at top: new components always insert at index 0, not at the end",
        "Seeded new projects: fresh projects seed with a plan component + a reference component (both at default widthFraction 1)",
        "Reference images natural aspect ratio: tiles render at per-image natural height (no 1:1 squashing); configurable per-component imageHeight (default 160, range 60–300, ±20 stepper, i18n reference.imageHeight label)",
        "Multi-select image insert: native file picker supports multiple file selection; images import in chosen order",
        "PDF export reveals folder: after save completes, the export-folder Rust command opens the parent directory in Explorer/Finder/native file manager",
        "SchemaVersion 3: ProjectPlan.schemaVersion incremented to 3; widthFraction now number; referenceComponents gain imageHeight; v2 → v3 migration converts 1/2/2/3 strings to 0.5/0.67/1, adds imageHeight: 160"
      ],
      "name": "Canvas UX Enhancements",
      "status": "completed",
      "progress": {
        "implementationPlan": "docs/superpowers/plans/2026-08-04-canvas-ux-enhancements.md",
        "lastVerified": {
          "unitTests": "336 passed",
          "e2eTests": "21 passed",
          "lint": "clean",
          "date": "2026-08-04",
          "rustTests": "30 passed",
          "build": "succeeded",
          "typecheck": "clean"
        },
        "designSpec": "docs/superpowers/specs/2026-08-04-canvas-ux-enhancements-design.md",
        "remaining": [],
        "completed": [
          "T1: Replaced fractional width presets with continuous drag (widthFraction number 0.4–1.0, quantized 0.01, persisted; double-click snap to presets; removed WidthResizeMenu)",
          "T2: Unified spacing constant (SPACING=24px) applied to all canvas margins, gaps, handle sizes",
          "T3: Extended drag handle to entire top bar (invisible handle for a11y, draggable parent); added low-emphasis type label (摄影计划/参考图组)",
          "T4: Four-edge + corner resize (left/right/top/bottom handles, ResizeDirection enum, useComponentResize recognizes 5 directions, width 0.4–1.0 / height 120–1000)",
          "T5: Delete confirmation dialog (AlertDialog wraps ComponentFrame delete, 确定删除该组件？ with 删除/取消)",
          "T6: Insert at top (insertComponent unshifts to index 0 instead of push)",
          "T7: Seeded new projects with plan + reference (EMPTY_PLAN_V3 includes one plan seeded with template, one reference '新建分组')",
          "T8: Reference images natural aspect ratio (SortableImageTile height: auto, max-h: imageHeight); per-component imageHeight stepper (±20, 60–300, i18n reference.imageHeight)",
          "T9: Multi-select image insert (Tauri dialog multiple: true, import_images loops file array, createReferenceImage sequential IDs)",
          "T10: PDF export folder reveal (export_canvas_to_pdf returns path, export_folder Rust command shell opens parent directory)",
          "T11: SchemaVersion 3 migration (CURRENT_SCHEMA_VERSION=3, migrateV2ToV3 converts widthFraction strings→numbers, adds imageHeight: 160, loadPlan chains v1→v2→v3)",
          "T12: e2e coverage (6 new tests: move by top bar reorders, resize width by right edge, insert at top, delete confirmation, adjust image height, export PDF success); featurelist updated; full validation matrix passed"
        ],
        "completedAt": "2026-08-04"
      }
    },
    {
      "name": "Theming + Settings",
      "feature_descriptions": [
        "Support light, dark, and system theme modes",
        "In-app settings panel accessible via gear button (设置) for theme selection",
        "Theme persistence in ~/.preshot/settings.json via Tauri commands (read_settings/write_settings)",
        "Tailwind v4 class-based dark mode (dark class on html element)",
        "Dark mode styling for BlockNote rich text editor",
        "Browser-based settings adapter for e2e tests (memory storage)",
        "Theme state managed via ThemeProvider with read/write to SettingsRepository",
        "Settings panel supports light (浅色), dark (深色), and system (跟随系统) theme selection",
        "Editorial Studio visual system with graphite global chrome, paper-white content, berry creation/export actions, functional-cyan editing states, and restrained 8px corners",
        "Bundled Outfit Variable display typography plus Noto Sans SC body typography with Lucide outline icons",
        "Globally persisted draggable project/assistant panel widths with keyboard operation, double-click reset, and safe min/max constraints",
        "Canvas-first default panel widths (192px project rail / 272px assistant) and pointer-anchored Ctrl+wheel A4 zoom with bounded 25%-150% scaling"
      ],
      "status": "completed",
      "decisions": [
        "Use Tailwind v4 class-based dark mode strategy with dark class on document root",
        "Store theme preference in AppSettings (domain model) with default 'system'",
        "Implement SettingsRepository port with Tauri and browser adapters",
        "Settings panel uses accessible buttons with aria-pressed for theme selection",
        "Theme changes apply immediately with dark class toggle on document.documentElement",
        "Wire settings adapter selection via VITE_WORKSPACE_ADAPTER environment variable",
        "E2E tests use browserSettingsRepository (memory storage) instead of Tauri commands",
        "System theme detection via window.matchMedia('(prefers-color-scheme: dark)')",
        "Use semantic app-* tokens for theme-aware shell chrome and invariant paper-* tokens for white A4 document content",
        "Keep the three-column desktop workbench expanded while constraining side widths so the A4 canvas remains the visual focus",
        "Use Lucide icons for scissors, settings, close, and rail navigation controls; preserve localized accessible names",
        "Persist the canonical visual rules in design-system/preshot/MASTER.md",
        "Persist project rail and assistant widths in AppSettings; preview locally during pointer drag and write once on release",
        "Use project rail 176–320px, assistant 240–420px, and a 480px canvas drag constraint; CSS safely degrades below the native 960px minimum window",
        "Cap automatic A4 scale at 0.92 so wide desktop windows remain creative workbenches rather than oversized reading views"
      ],
      "progress": {
        "completedAt": "2026-08-04",
        "visualRefreshCompletedAt": "2026-08-08",
        "implementationPlan": "docs/superpowers/plans/2026-08-04-theming.md",
        "designSpec": "docs/superpowers/specs/2026-08-04-theming-design.md",
        "designSystem": "design-system/preshot/MASTER.md",
        "completed": [
          "T1: Domain models and ports (AppSettings, Theme type, SettingsRepository interface)",
          "T2: Settings persistence (read_settings/write_settings Rust commands, tauriSettingsRepository adapter)",
          "T3: Browser settings adapter (in-memory browserSettingsRepository for e2e)",
          "T4: ThemeProvider with system detection and dark class application",
          "T5: Settings UI (SettingsButton, SettingsPanel with theme selection)",
          "T6: Wire settings adapter for e2e (settingsDependencies.ts selects adapter based on VITE_WORKSPACE_ADAPTER)",
          "T7: E2E tests for theme switching (theme.spec.ts with light/dark/system toggle verification)",
          "T8: Featurelist update and full validation matrix",
          "2026-08-08: applied the Clear Editorial system to the app shell, A4 canvas, image controls, dialogs, launcher, project cards, overlays, statuses, and error surfaces",
          "2026-08-08: refined component delete controls with a soft paper-danger state and replaced the screenshot glyph with a Lucide scissors icon",
          "2026-08-08: bundled Nunito Sans Variable, reused bundled Noto Sans SC, added reduced-motion handling, and removed legacy stone/amber classes from user-facing TSX",
          "2026-08-08: confirmed and implemented Editorial Studio from docs/design_refs/preshot-editorial-hifi.html: graphite top bar, cover-based project rail, berry actions, cyan editing states, grid canvas, floating insert toolbar, and neutral A4 paper",
          "2026-08-08: bundled Outfit Variable and added global pointer/keyboard-resizable side panels with persisted widths and double-click defaults",
          "2026-08-08: increased the default center-column share by narrowing side-panel defaults, migrated the former 208/304 default pair, and added non-passive Ctrl+wheel canvas zoom in exact 10% steps while preserving ordinary wheel scrolling"
        ],
        "lastVerified": {
          "date": "2026-08-08",
          "unitTests": "77 files, 429 tests passed",
          "e2eTests": "38 Edge tests passed",
          "typecheck": "clean",
          "lint": "0 errors; existing ThemeProvider Fast Refresh warning remains",
          "rustTests": "not rerun; no native code changed",
          "build": "succeeded with bundled Outfit/Noto/Nunito fonts and existing Vite chunk-size warning",
          "visualAcceptance": "1600x1000 light and 960x720 dark remain accepted; at 1200x800 the default columns are 192/6/724/6/272px with no horizontal overflow, Ctrl+wheel changes A4 width 547.66px -> 602.42px (110%), ordinary wheel preserves zoom, and reverse Ctrl+wheel restores 547.66px"
        },
        "remaining": []
      }
    },
    {
      "name": "Storage Model",
      "feature_descriptions": [
        "Converge project files on a .preshotproj manifest (renamed from .preshot), keeping the versioned JSON shape and embedded canvas plan unchanged",
        "Auto-migrate legacy .preshot projects to .preshotproj on open/inspect (atomic write, then remove the legacy file; a removal failure keeps the migration)",
        "Default the new-project parent-folder picker to ~/.preshot/projects (created on demand), with Chinese dialog titles/labels via i18n",
        "Resolve duplicate project names in the same parent to the first free ' (2)', ' (3)', … suffix so folder and display name stay in sync",
        "Keep the ~/.preshot home folder and settings.json path unchanged (only the per-project manifest filename moved to .preshotproj)"
      ],
      "status": "completed",
      "decisions": [
        "Rename the per-project manifest to .preshotproj; migrate legacy .preshot one-way on read via read_manifest (validate before writing the new file)",
        "Add pure, unit-tested Rust helpers dedupe_project_name(parent, name) and default_projects_dir; create_project_in dedupes instead of erroring on a name collision and stores the resolved name in the manifest",
        "Expose a #[tauri::command] default_projects_dir returning ~/.preshot/projects for the picker default (created with create_dir_all)",
        "Keep the invoke('default_projects_dir') call inside the infrastructure directory picker; the app layer only signals intent via a { defaultToProjectsDir: true } option to preserve the layer boundary",
        "Localize picker titles through the zh i18n dictionary (picker.createParent / openProject / relocate with {{name}} interpolation)",
        "e2e stays a smoke layer on the memory adapter (which cannot create on disk); on-disk migration and dedupe are covered by Rust unit tests"
      ],
      "progress": {
        "completedAt": "2026-08-04",
        "implementationPlan": "docs/superpowers/plans/2026-08-04-storage-model.md",
        "designSpec": "docs/superpowers/specs/2026-08-04-storage-model-design.md",
        "completed": [
          "T1: .preshotproj manifest constant + LEGACY_MANIFEST_FILE_NAME with one-way migrating read_manifest (+3 migration tests)",
          "T2: dedupe_project_name + default_projects_dir command; create_project_in dedupes and stores the resolved name (+5 tests)",
          "T3: new-project picker defaults to ~/.preshot/projects via a directory-picker option; zh i18n picker titles; .preshot→.preshotproj mock-message references",
          "T4: featurelist update and full validation matrix"
        ],
        "lastVerified": {
          "date": "2026-08-04",
          "unitTests": "380 passed",
          "e2eTests": "23 passed",
          "typecheck": "clean",
          "lint": "clean",
          "rustTests": "44 passed",
          "build": "succeeded"
        },
        "remaining": []
      }
    },
    {
      "name": "Undo / Redo",
      "feature_descriptions": [
        "Session-scoped structural undo/redo for the canvas via 撤销/重做 toolbar buttons and Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y",
        "Structural-only scope: add/remove/move/resize components, image add/remove/reorder/caption, image height, group title, and description visibility are undoable",
        "Rich-text editing (plan body and reference description) is left to BlockNote's own Ctrl+Z; the global handler never fights the editor",
        "On undo/redo the structural snapshot is restored while the CURRENT html/description of components surviving by id is preserved (structural undo never rewinds typed text)",
        "Capped (~50) in-memory snapshot history, reset on each project open; never persisted",
        "Consecutive same-target resize/image-height changes within a short window coalesce into a single undo entry"
      ],
      "status": "completed",
      "decisions": [
        "Pure snapshot history in src/domain/plan/canvas/history.ts (createHistory/record/undo/redo/canUndo/canRedo) — no React/DOM/Tauri, deep-copied snapshots, never throws",
        "mergeStructural(target, current) keeps current html/description for components surviving by id+type so a structural undo preserves BlockNote text",
        "ProjectCanvasProvider (the single applyPlan mutation point) records the previous plan via a history-aware mutate helper for structural handlers; text handlers (handleChangeHtml/handleSetDescription) stay on plain applyPlan",
        "Coalesce resize bursts with key resize:<id> and image-height with imageHeight:<id> within COALESCE_WINDOW_MS (600ms)",
        "Keyboard: window keydown routes Ctrl+Z to BlockNote when a contenteditable/INPUT/TEXTAREA/.bn-editor/.ProseMirror is focused, else global undo; Ctrl+Shift+Z / Ctrl+Y redo",
        "History is per-session (reset on load, not persisted); async image handlers capture prev at the pre-change point and record exactly once on success",
        "Removal undo is reference-level: removeImage/removeComponent eagerly delete backing files, so undo of a removal is correct in-session (renders from the imageSrc cache) but not durable across a save+reload (a restored image points at a deleted file). Durable remove-undo needs save-time orphan collection / session trash — tracked as a Storage follow-up."
      ],
      "progress": {
        "completedAt": "2026-08-04",
        "implementationPlan": "docs/superpowers/plans/2026-08-04-undo-redo.md",
        "designSpec": "docs/superpowers/specs/2026-08-04-undo-redo-design.md",
        "completed": [
          "T1: pure history stack (createHistory/record/undo/redo/canUndo/canRedo with capping + coalescing)",
          "T2: mergeStructural text-preserving restore",
          "T3: provider history integration (mutate + undo/redo + reset on load + canUndo/canRedo)",
          "T4: keyboard shortcuts with contenteditable/editor focus routing",
          "T5: 撤销/重做 toolbar buttons + zh i18n history keys + disabled-state coverage",
          "T6: e2e smoke (keyboard + toolbar insert/undo/redo) + featurelist + full matrix"
        ],
        "lastVerified": {
          "date": "2026-08-04",
          "unitTests": "401 passed",
          "e2eTests": "25 passed",
          "typecheck": "clean",
          "lint": "clean",
          "rustTests": "44 passed",
          "build": "succeeded"
        },
        "remaining": [
          "Durable remove-undo: defer physical image-file deletion to a save-time orphan collection or session trash so undoing an image/component removal survives a save+reload (Storage follow-up)."
        ]
      }
    },
    {
      "name": "Windows MSI Installer",
      "feature_descriptions": [
        "pnpm tauri:build on Windows emits a WiX MSI (Preshot_<ver>_x64_en-US.msi)",
        "Installs to Program Files (per-machine) with a Start Menu shortcut and a Desktop shortcut, plus an Add/Remove Programs uninstaller",
        "~/.preshot is created by the app at first run (theming settings command), not by the installer",
        "Unsigned installer (SmartScreen warning acceptable this phase); no system PATH modification; en-US installer UI"
      ],
      "status": "completed",
      "decisions": [
        "Use Tauri's WiX (WiX Toolset v3) MSI target — bundle.targets=[\"msi\"] — not NSIS, matching the PRD's .msi requirement",
        "Tauri v2's default WiX template already defines and ships ApplicationShortcutDesktop (Desktop) + ApplicationShortcut (Start Menu) + an uninstall shortcut via its ShortcutsFeature, so NO custom WiX fragment is needed; a shortcuts.wxs redefining ApplicationShortcutDesktop fails the linker with LGHT0091 (duplicate symbol)",
        "Set bundle.publisher=yucdong (WiX Manufacturer) and bundle.windows.wix.language=[\"en-US\"]",
        "A Vitest config-contract test (src/app/packaging/msiConfig.test.ts) guards targets=[\"msi\"], publisher, and en-US so the bundle config can't silently drift",
        "Unsigned MSI for now; per-machine install with per-user ~/.preshot at runtime; no PATH change"
      ],
      "progress": {
        "completedAt": "2026-08-05",
        "implementationPlan": "docs/superpowers/plans/2026-08-04-msi-installer.md",
        "designSpec": "docs/superpowers/specs/2026-08-04-msi-installer-design.md",
        "completed": [
          "T1: WiX MSI bundle config (targets/publisher/en-US) + config-contract test; discovered Tauri ships the Desktop shortcut by default and removed the redundant custom fragment",
          "T2: build verification (pnpm tauri:build produced Preshot_0.1.0_x64_en-US.msi, 16.9 MB) + featurelist + full matrix"
        ],
        "lastVerified": {
          "date": "2026-08-05",
          "unitTests": "404 passed",
          "e2eTests": "25 passed",
          "typecheck": "clean",
          "lint": "clean",
          "rustTests": "44 passed",
          "build": "succeeded",
          "msiBuild": "Preshot_0.1.0_x64_en-US.msi (16.92 MB) at src-tauri/target/release/bundle/msi/"
        },
        "remaining": [
          "Code-sign the MSI to remove the SmartScreen warning (cert-dependent follow-up).",
          "Optional zh-CN installer UI language and branded banner/dialog art.",
          "Run the manual install checklist on a clean Windows box (install, both shortcuts launch, ~/.preshot created, uninstall removes app + shortcuts)."
        ]
      }
    },
    {
      "name": "Adaptive Component Layout",
      "feature_descriptions": [
        "Canvas plans move to persisted schemaVersion 4 for adaptive layout work",
        "Persist only component width; component height becomes derived layout state",
        "Reference components default to imageHeight 135 and legacy v3 image heights are reduced once during migration",
        "Plan and reference components grow from measured content without large fixed blank areas",
        "Reference image flow keeps full uncropped aspect ratios, wraps by row boundaries, and carries caption height in layout",
        "One BlockNote plan editor can span multiple A4 pages via runtime spacer metadata instead of persisted fragment HTML",
        "Long reference descriptions keep one BlockNote editor while runtime-only spacers paginate text before image rows and following components",
        "Component resize is width-only; top/bottom/corner height handles are removed from the canvas chrome",
        "Drag-and-drop shows live placeholders plus compact overlays and respects reduced-motion preferences",
        "PDF export resolves every expected reference image before rendering and aborts rather than producing a partial document",
        "The former v5 canvas model is superseded by schema v6 automatic packing, content scaling, per-image display sizing, and independent captions"
      ],
      "status": "completed",
      "decisions": [
        "Task 1 establishes BaseComponent as { id, width } and removes persisted component height from ProjectPlan schema v4",
        "migratePlan upgrades schemaVersion 3 payloads to v4 by dropping height and applying clampImageHeight(oldImageHeight * 0.75) exactly once",
        "Width-only resizing is enforced across the domain reducer and canvas UI so later adaptive-layout tasks can rely on derived heights",
        "Measured BlockNote plan bodies and reference descriptions stay runtime-only, so adaptive heights and page-break spacers never persist into plan history or schema v4 saves",
        "Reference row packing and pagination keep images proportional and uncropped, reserve caption bands in the same slot model, and split only at row boundaries for UI/PDF parity",
        "PlanCanvas now renders component fragments on a single PagedCanvasSurface global-Y coordinate system, and left-handle preview keeps the right edge visually fixed while only width persists",
        "Task 7 uses dnd-kit sortable overlays with a 180ms pointer delay, unique fragment-level image-group droppable ids mapped back to logical component ids, and reduced-motion-aware 200ms layout easing",
        "Task 8 keeps PDF reference-image export keyed by ReferenceFlowSlot.id, reuses the shared zh continuation-title template, and validates exact adaptive slot geometry across continuation fragments",
        "Task 9 seeds the browser memory adapter with four stable landscape/portrait demo images and keeps drag smoke coverage aligned with the >180ms activation delay required by adaptive sortable overlays",
        "Final review makes plan loading lossless: repository null is a typed missing result, stored empty v4 plans remain empty, malformed/future non-null plans throw contextual errors, and invalid image ratios normalize to 1 without dropping valid image records",
        "Final review shares explicit point geometry for editable frame chrome and reference headers; the UI renders every vertical reservation at points × scale while PDF passes zero editing chrome",
        "Final review replaces bounded hydration retries with an identity-filtered, coalesced MutationObserver that watches top-level BlockNote group/direct-block replacement until cleanup without reacting to attribute/style churn",
        "Final review measures and paginates PDF rich text from the same extracted tokenization/line metrics used for drawing, excludes the UI add tile from PDF row flow, and contain-fits embedded images using their actual dimensions",
        "Final re-review applies the same identity-filtered BlockNote measurement hook to reference descriptions; the first logical fragment may span page surfaces, then complete image rows start on safe continuation pages",
        "Final re-review snapshots the current plan, loads every missing expected image before export, and validates the complete file/component/image map again inside the PDF exporter",
        "Ultimate review scopes loading/ready/failed lifecycle and async generations to projectPath, so stale loads cannot replace a newer project and loading or failed plans cannot mutate, save, export, or respond to undo/redo shortcuts",
        "Ultimate review rebases aspect-ratio hydration onto the latest plan, updates every shared-file reference without adding history, and leaves the exact backfilled snapshot unsaved until autosave or flush persists it",
        "Ultimate review enforces globally unique component/image logical ids while preserving legal duplicate file references, and deletes physical image files only after the resulting plan proves them orphaned",
        "Ultimate review adds one numeric-id canvas droppable and filters collision fallbacks by exact pointer containment, while unchanged sync/async mutations skip history and save-state changes",
        "Final blocker fix captures and flushes a ready, dirty old-project snapshot before projectPath generation invalidation, while stale loads remain unable to apply after the switch",
        "Final blocker fix deterministically suffix-remaps colliding v1/v2 component and image ids while v3/v4 stored plans continue to reject duplicate logical ids",
        "Final retirement fix tracks each ready token's latest persistence, rebases an in-flight old-project mutation onto an explicit retired plan snapshot, and only then performs the fail-safe dirty save without exposing stale errors to the replacement project",
        "The former v5 manual-arrangement and crop behavior is superseded by schema v6; current persistence uses flat ordered components, automatic packing, displayHeight, and contentScale.",
        "Current retirement handling rebases completed async image/component deltas onto the latest persistence snapshot before serialized save and blocks conflicting destructive operations."
      ],
      "progress": {
        "designSpec": "docs/superpowers/specs/2026-08-05-adaptive-component-layout-design.md",
        "implementationPlan": "docs/superpowers/plans/2026-08-05-adaptive-component-layout.md",
        "completedAt": "2026-08-06",
        "completed": [
          "Task 1: implemented schemaVersion 4, removed persisted component heights from v4 plan/component types, and updated fixtures to compile honestly against the new model",
          "Task 1: migrated legacy v3 reference image heights to the new 135pt baseline by scaling old values once during v4 migration",
          "Task 1: removed top/bottom/corner component resize handles so component resizing is width-only in the canvas UI",
          "Task 2: added pure reference row packing and pagination utilities with scaled add-tile handling for narrow layouts",
          "Task 3: derived adaptive A4 placements and reference fragments in the engine and reused them in PDF layout/export paths",
          "Task 4: replaced per-page positioning roots with a single PagedCanvasSurface, rendered fragment frames in global Y coordinates, and fixed left-handle preview anchoring while keeping width-only resize persistence",
          "Task 5: added runtime-only natural height maps for BlockNote plan bodies and reference descriptions, scoped them per project path, and kept them out of persistence plus undo history",
          "Task 5: added BlockNote top-level block measurement with runtime page-break spacer classes/CSS metadata so one editor can span multiple A4 pages without rewriting stored HTML",
          "Task 7: switched component frames to one logical sortable identity per component, gave paginated reference fragments unique image-group droppable ids, added compact DragOverlay previews plus source placeholders, and kept live image/component preview reflow reduced-motion aware",
          "Task 8: reused the adaptive reference fragment slots in canvasPdfExporter, centralized the zh continuation-title template for UI/PDF parity, and added multipage PDF regressions for exact image geometry plus continuation-slot id sequencing",
          "Task 9: added focused Playwright smoke coverage for natural content growth, multi-row non-scrolling proportional reference flow, and drag placeholder/overlay commit invariants; aligned legacy canvas drag gestures with the required >180ms overlay activation delay",
          "Final review: added strict migration/service/provider load regressions and a typed missing-vs-loaded result so unreadable stored data is never replaced or auto-saved",
          "Final review: unified scaled frame/reference geometry, pinned all component/image source placeholders during drag, and replaced bounded BlockNote hydration polling with filtered event-driven identity observation",
          "Final review: added adaptive PDF text measurement and multi-page drawing, PDF-only add-tile exclusion, actual-image contain fitting, valid browser PNG fixtures, stronger PDF integration coverage, and exact E2E frame-containment checks",
          "Final re-review: paginated long reference descriptions through one runtime-measured BlockNote editor, moved image rows to safe continuation fragments, and kept following UI/PDF components after the final fragment",
          "Final re-review: made export await the exact current-plan image set, surface contextual load failures without saving, reject missing exporter entries, and require exact seeded-image readiness in browser PDF smoke tests",
          "Ultimate review: added fail-closed provider lifecycle and generation-token regressions for malformed loads, project switches, autosave, keyboard shortcuts, mutation, and export paths",
          "Ultimate review: made delayed aspect hydration preserve concurrent text/structure edits, update every reference to a shared file, avoid history, and remain unsaved until the exact snapshot is flushed",
          "Ultimate review: rejected duplicate component and globally duplicate image ids in v3/v4 migration with positional context while keeping repeated files valid",
          "Ultimate review: made image/component removal reference-aware after computing the next plan and deduplicated physical deletes",
          "Ultimate review: constrained collision fallback to exact continuous-canvas containment and prevented unchanged synchronous or asynchronous structural results from changing history/save state",
          "Ultimate review: aligned browser seed ratios to exact image dimensions so hydration is a no-op for the stable E2E fixture",
          "Final blockers: preserved dirty old-project edits during in-place project switches with a captured pre-invalidation save, while clean snapshots skip the write",
          "Final blockers: migrated legacy v1/v2 duplicate component and image identities to deterministic unique suffixes without losing records or file references, retaining strict v3/v4 rejection",
          "Final retirement blocker: waited for old-token import/remove/flush persistence before rebasing and saving the latest retired plan, preserving physical deletion ordering and preventing stale completion state or errors from reaching the new project UI",
          "Historical v5 canvas integration is superseded by the schema v6 completion entry.",
          "Current retirement handling rebases delayed image/component operations before a serialized latest-snapshot save.",
          "Current acceptance coverage uses automatic packing, per-image display-height reset, independent captions, description visibility, and keyboard history."
        ],
        "lastVerified": {
          "date": "2026-08-07",
          "targetedE2e": "pnpm test:e2e -- e2e/canvas.spec.ts e2e/undo-redo.spec.ts e2e/theme.spec.ts: 24 passed",
          "postReviewTargetedUnits": "63 tests passed: planDependencies/browserPlan (11), CanvasTitle/ComponentFrame (18), migrate (34)",
          "postReviewTargetedE2e": "Superseded by schema-v6 browser smoke coverage",
          "pnpmLint": "passed (exit 0); unchanged ThemeProvider Fast Refresh warning remains",
          "pnpmTypecheck": "passed",
          "pnpmTest": "74 files, 639 tests passed",
          "pnpmTestInit": "confirmed unchanged baseline failure: fixture still does not match init.ps1's actionable pnpm failure text",
          "pnpmTestE2e": "30 passed",
          "cargoTest": "44 passed",
          "pnpmBuild": "passed with the existing Vite >500 kB chunk-size warning"
        },
        "remaining": [
          "The unchanged test:init fixture assertion mismatch remains outside this Task 8 review scope."
        ]
      }
    },
    {
      "name": "Canvas Layout and Document Chrome History (superseded by v6)",
      "status": "completed",
      "feature_descriptions": [
        "Historical predecessor of the schema-v6 canvas model",
        "Editable canvas and component names",
        "PDF title and caption parity precursor",
        "Global settings and refined theme-aware canvas chrome"
      ],
      "decisions": [
        "Superseded by schema v6, which persists flat component order and derives packing after every geometry change.",
        "Current image interaction uses per-image displayHeight with reset rather than historical image geometry metadata.",
        "Source images remain unchanged and lightbox rendering uses the original source.",
        "Final 0806 review: key retirement coordination by CanvasPlanService in a WeakMap, queue every project retirement immediately, and make fresh/remounted providers await matching and service-ordered barriers before loading.",
        "Final 0806 review: preserve the service-persisted aspect-ratio placeholder as the save baseline; apply batch-hydrated image ratios afterward as unsaved metadata so timer and retirement persistence write the measured values.",
        "Final 0806 remediation: keep deferred single and batch decode measurement inside the tracked persistence operation, update the retired persistence plan even when its UI token is stale, and apply it to React only while that token remains current.",
        "Final 0806 single-import follow-up: decode the imported data URL and update the tracked persistence plan before the current-token UI gate, so retirement saves the measured ratio after a deferred rebase while stale React state and history remain untouched.",
        "Current project-retirement persistence waits for deferred image operations and saves the latest rebased schema-v6 snapshot without leaking completion into the next project.",
        "Final 0806 re-review: make every successor load await the retiring project's complete persistence plus latest-plan save before it enters the shared service FIFO; preserve generation-token isolation, surface retirement-save failures instead of loading stale disk state, and clear the failed barrier after that error so a later explicit selection can recover.",
        "Final 0806 unmount review: launcher/new-project unmount retirement serializes behind prior retirements, waits for pending destructive persistence to rebase its latest plan, and writes that plan once without setting unmounted React state; per-project saved snapshots prevent re-saving completed service operations or in-flight saves, while retirement errors retain contextual logging.",
        "Schema-v6 packing shares one gap-aware layout engine across screen and PDF.",
        "Late-page reference components move intact when their first complete image row cannot fit.",
        "Schema-v6 document titles reserve the same screen/PDF band while component names use editable, unique labels."
      ],
      "verification": {
        "targetedE2e": "same-row insertion and row-target smoke: 2 passed; crop preview/persistence smoke: 2 passed",
        "postReviewTargetedUnits": "63 passed",
        "postReviewTargetedE2e": "cropped-tile lightbox source assertion: 1 passed",
        "finalRereviewTargetedUnits": "WorkspaceProvider, ProjectCanvasProvider, PlanCanvas, and rowDropZoneGeometry: 108 passed",
        "finalRereviewTargetedE2e": "pnpm exec playwright test e2e/canvas.spec.ts --grep \"renders first and last new-row drop targets\": 1 passed",
        "finalReviewRetirementTargetedUnits": "projectRetirementCoordinator, ProjectCanvasProvider, WorkspaceProvider, and canvas service: 79 passed",
        "finalReviewRetirementTargetedE2e": "pnpm exec playwright test e2e/canvas.spec.ts e2e/workspace.spec.ts: 25 passed",
        "finalReviewDecodeCropTargetedUnits": "WorkspaceProvider, ProjectCanvasProvider, and ImageCropOverlay: 83 passed",
        "finalReviewDecodeCropTargetedE2e": "pnpm exec playwright test e2e/canvas.spec.ts e2e/workspace.spec.ts: 25 passed",
        "finalSingleImportRetirementTargetedUnits": "ProjectCanvasProvider and workspace storage/dialog tests: 78 passed",
        "finalSingleImportRetirementTargetedE2e": "pnpm exec playwright test e2e/canvas.spec.ts e2e/workspace.spec.ts: 25 passed",
        "pnpmLint": "passed (exit 0; unchanged ThemeProvider Fast Refresh warning)",
        "pnpmTypecheck": "passed",
        "pnpmTest": "75 files, 662 tests passed",
        "pnpmTestInit": "passed: 4 initializer scenarios",
        "pnpmTestE2e": "33 passed",
        "cargoTest": "44 passed",
        "pnpmBuild": "passed",
        "jsonParse": "passed",
        "gitDiffCheck": "passed"
      }
    },
    {
      "name": "Reference Image Multi-Select, Import Progress, and Screen Capture",
      "status": "completed",
      "feature_descriptions": [
        "Single click selects a reference image without opening it; double click opens the original image lightbox",
        "Ctrl+click toggles selection across image groups and dragging a selected image moves the full selection in canvas order",
        "Rejected duplicate or empty component names restore the last committed name",
        "Batch image import shows determinate progress, retains successful imports, continues after individual failures, and reports a final summary",
        "Each image group includes a screenshot action that opens Windows native screen clipping and imports the new clipboard image",
        "Reference image groups continue across pages without rendering repeated “name（续）” headings",
        "Import and screenshot controls live on the left of the image-size toolbar with hover descriptions and a monochrome screenshot icon; size controls stay on the right"
      ],
      "decisions": [
        "Keep image selection ephemeral in PlanCanvas; selection is not persisted in schema v6",
        "Use a pure moveImages reducer and selected-image drop target so one history entry moves the ordered batch within or across groups",
        "Use per-file CanvasPlanService.importImage calls for batch progress and partial-failure continuation while rebasing successes onto current metadata",
        "Model Windows capture as start/poll/cancel domain commands; compare clipboard sequence numbers so stale clipboard images are never imported",
        "Write native RGBA clipboard data to a unique temporary PNG and reuse the existing reference-image import path, which copies it into the project",
        "Cancel sends Escape to dismiss Windows screen clipping and generation checks prevent an in-flight poll result from importing after cancellation",
        "Continuation fragments reserve no repeated-title header in either the canvas layout or PDF export",
        "The image display area contains only images and an empty-group drop target; it never renders import or screenshot buttons"
      ],
      "verification": {
        "focusedUnits": "9 files, 205 tests passed",
        "canvasE2e": "26 passed",
        "pnpmLint": "passed (exit 0; unchanged ThemeProvider Fast Refresh warning)",
        "pnpmTypecheck": "passed",
        "pnpmTest": "76 files, 677 tests passed",
        "pnpmTestInit": "passed: 4 initializer scenarios",
        "pnpmTestE2e": "35 passed",
        "cargoTest": "47 passed",
        "pnpmBuild": "passed",
        "continuationAndControlsFocusedUnits": "5 files, 94 tests passed",
        "e2eFollowUp": "34/35 passed in the parallel full run; the existing same-row drag gesture passed on isolated retry"
      },
      "remaining": []
    },
    {
      "name": "Canvas Schema v6 Completion",
      "description": "Completes the approved v6 canvas model: flat automatic component packing, four-edge component scaling, per-image display sizing, independent captions, visible-description hiding, Windows capture, and screen/PDF layout parity.",
      "status": "completed",
      "keyDecisions": [
        "Persist a flat ordered component list only; widths and page rows are derived by the pure layout engine after every resize or move.",
        "Use contentScale for component content at every rendering boundary; PDF preserves the stored scale while its synthetic document-title spacer remains scale 1.",
        "Store a per-image displayHeight override and remove it on reset; captions belong to individual images and description visibility belongs only to the group description.",
        "Keep the reference first image row with its header when pagination begins late, rather than creating a header-only fragment.",
        "Use ScreenCapture start/poll/cancel ports with Windows clipboard-sequence validation, then reuse the normal image import path.",
        "Size component-name inputs from Unicode-aware text-width estimates so CJK, full-width, emoji, and Latin names remain fully visible without consuming the entire drag bar."
      ],
      "migration": [
        "Migrate schemas v1 through v5 into schemaVersion 6.",
        "Discard obsolete persisted row, crop, and group-caption-toggle metadata while preserving component names, descriptions, images, and individual captions.",
        "Default contentScale to 1, showDescription to true, and image displayHeight to unset for migrated records."
      ],
      "verification": {
        "pnpmLint": "passed with 0 errors; one existing ThemeProvider Fast Refresh warning",
        "pnpmTypecheck": "passed",
        "pnpmTest": "73 files, 630 tests passed",
        "pnpmTestInit": "4 initializer harness checks passed",
        "pnpmTestE2e": "34 Edge smoke tests passed",
        "cargoTest": "47 Rust tests passed with vcvars64",
        "pnpmBuild": "passed; Vite emitted its existing large-chunk warning"
      },
      "remaining": [
        "If native capture writes its temporary PNG successfully but the following import fails, the source remains for OS cleanup/manual recovery."
      ]
    },
    {
      "name": "A4 Document Flow Canvas",
      "description": "Replaces the schema-v7 continuous free canvas with a print-first A4 document flow where adjustable-size components occupy exclusive rows, reorder one position through edge arrow controls, never cross pages, and create persisted continuation components when content overflows.",
      "status": "completed",
      "feature_descriptions": [
        "Screen canvas renders exact A4 sheets and printable margins for screen/PDF WYSIWYG parity",
        "Every component occupies an exclusive row while retaining adjustable width and height",
        "Component edge arrows move cards one array position; manual component dragging is disabled while image dragging remains available",
        "Components move intact to the next page when the current page has insufficient height",
        "Reference groups shrink images to a 67.5pt minimum before creating persisted (2), (3), ... continuation groups",
        "Plan text splits into persisted continuation components at BlockNote block boundaries without reducing font size",
        "Plan text cards shrink and grow to their measured BlockNote content, including numbered-list wrapping, without internal page spacer margins",
        "Reference-card borders shrink and grow to the final packed image row so compact consecutive groups can share an A4 page",
        "Reference toolbars provide 24pt-minimum, 4pt-step proportional whole-group image-height buttons with a dynamic one-page maximum, card resizing, and automatic page reflow",
        "Historical schema-v7 continuous-canvas cards taller than one A4 page migrate safely before v8 normalization",
        "Android-compatible Chinese PDF output using valid document-specific TrueType Noto Sans SC Regular/Bold subsets with ToUnicode maps",
        "PDF reference images are downsampled to their largest actual placement at 144 DPI and encoded as 76% JPEG instead of embedding full-resolution originals"
      ],
      "decisions": [
        "Introduce schema v8 with persisted x/width/height and array order, removing free vertical y",
        "Migrate v7 once by stable y/x/source-index sorting so existing visual order is retained",
        "Persist continuation components as normal editable, sortable, undoable project components",
        "Share A4 geometry, exclusive-row ordering, persisted continuations, and horizontal offsets between screen and PDF while the retained PDF engine is retired incrementally",
        "Treat oversized unsplit components as an explicit normalization error rather than allowing cross-page rendering",
        "Validate source schemas according to their own geometry: v7 accepts finite continuous-canvas heights, then v7-to-v8 clamps to A4 before continuation normalization; v8 remains strict",
        "Use compact 20pt×17pt hover/focus-revealed Lucide up/down controls with visible 上移/下移 labels, fully outside the left card border with a 2pt gap; first/last boundaries use native disabled state and component DnD code is removed",
        "Use average frameHeight as the group value; minus/plus commit one proportional 4pt scale mutation, preserve per-image frame ratios, and disable growth at the calculated one-page maximum",
        "Compress each unique PDF image once using the maximum final placement across all pages; preserve aspect ratio, never upscale, flatten transparency against paper white, and use browser Canvas high-quality resampling",
        "Generate document-specific TTFs with fonteditor-core from the exact plan characters, then embed with pdf-lib subset:false; direct fontkit TrueType subsetting is forbidden because it emits malformed CJK glyf records"
      ],
      "progress": {
        "designSpec": "docs/superpowers/specs/2026-08-08-a4-document-flow-design.md",
        "compatibilitySpec": "docs/superpowers/specs/2026-08-08-schema-v7-oversized-card-compatibility.md",
        "textAutoSizeSpec": "docs/superpowers/specs/2026-08-08-text-card-natural-height.md",
        "referenceAutoSizeSpec": "docs/superpowers/specs/2026-08-08-reference-card-natural-height.md",
        "referenceGroupScalingSpec": "docs/superpowers/specs/2026-08-08-reference-group-scaling.md",
        "arrowOrderingRequirement": "docs/design_docs/requirement_01.md",
        "implementationPlan": "docs/superpowers/plans/2026-08-08-a4-document-flow.md",
        "completed": [
          "Clarified width/height retention, persisted continuations, 67.5pt image minimum, and BlockNote block splitting",
          "Added the pure layoutDocumentFlow foundation for exclusive rows and intact A4 page moves",
          "Introduced strict schema v8 and migrated v1-v7 through stable y/x/source-index ordering while removing persisted y",
          "Restored an active A4 paged surface fitted to the middle panel without horizontal overflow",
          "Replaced coordinate dragging with sortable component order, source placeholders, compact overlays, and one-entry undo history",
          "Added reference-group natural sizing, 67.5pt uniform scaling, complete-row splitting, and persisted uniquely named continuation groups",
          "Added BlockNote-owned top-level block HTML serialization, DOM block measurement, and persisted text continuations",
          "Removed plan/reference internal scrolling so normalized component height contains all visible content",
          "Preserved exclusive rows and horizontal offsets in PDF export and kept complete-image readiness validation",
          "Aligned browser smoke coverage with the approved removal of image captions, description hiding, and the group image-height stepper",
          "Fixed schema-v7 oversized-card compatibility: continuous-canvas heights above one A4 page now reach the existing v8 clamp and continuation normalization instead of failing source-schema validation",
          "Validated the complete 小清新人像拍摄 manifest in memory, then preserved its original schema-v7 manifest as a verified backup before live migration",
          "Made fitting plan components converge bidirectionally to natural BlockNote height and retired legacy in-editor page spacer injection",
          "Added generation-tagged BlockNote block serialization so stale block HTML cannot be paired with newer DOM measurements and duplicate continuation content",
          "Added optional validated v8 plan contentScale with compensated screen zoom and PDF adapter parity",
          "Persisted loaded-component height normalization immediately without adding undo history, while inserted components and later user edits retain normal unsaved behavior",
          "Merged all 小清新人像拍摄 (2)/(3) components into their bases using the user-selected force-fit policy; retained 7 components and 0 numbered continuations",
          "Made fitting reference cards converge bidirectionally to the final packed image-row bottom; compacted 延安西路/夏日西瓜 to 297.77pt/397.38pt so each shares an A4 page with its adjacent plan card",
          "Added minus/value/plus controls after reference import/capture: 24pt minimum, exact 4pt steps, and a domain-calculated maximum that keeps the full group on one A4 page",
          "Removed component useSortable, component collision/drop helpers, drag handles/placeholders/overlays, and replaced them with hover/focus-revealed labeled one-position up/down edge controls",
          "2026-08-09: replaced CFF OpenType Noto Sans SC assets with OFL static TrueType 400/700 instances, then diagnosed that direct fontkit TrueType subsetting still corrupts CJK glyf records after the user supplied a rendered failure screenshot",
          "2026-08-09: added fonteditor-core document-character subsetting and changed pdf-lib to embed the finished Regular/Bold TTFs with subset:false; the real browser-exported 36,994-byte diagnostic PDF renders all Chinese title/body/list text continuously and extracts exact Unicode in Poppler",
          "2026-08-09: replaced full-resolution PDF image embedding with placement-aware 144 DPI JPEG compression at 76% quality; a 2400×1600 3.93 MB PNG rendered into a 100×75pt slot became a 200×133 6.8 KB JPEG in Chromium, reducing the complete comparison PDF from 1,452,901 bytes to 32,393 bytes (97.8%)"
        ],
        "lastVerified": {
          "date": "2026-08-09",
          "pnpmLint": "passed with 0 errors; one existing ThemeProvider Fast Refresh warning",
          "pnpmTypecheck": "passed",
          "pnpmTest": "79 files, 434 tests passed",
          "pnpmTestE2e": "39 Edge smoke tests passed",
          "pnpmTestInit": "4 initializer harness checks passed",
          "cargoTest": "47 Rust tests passed via ~/.cargo/bin/cargo.exe",
          "pnpmBuild": "passed with static TrueType Noto Sans SC assets and the existing Vite >500 kB chunk-size warning",
          "androidPdfCompatibility": "Both document-specific CJK weights embed as valid CIDFontType2 fonts with Identity-H encoding and ToUnicode maps; tests extract both FontFile2 streams and verify every expected Chinese glyph has a nonempty outline; Poppler rendering and exact text extraction passed on a real browser export",
          "pdfImageCompression": "Each unique source is downsampled once to its largest final draw box at 144 DPI, flattened to paper white, and encoded as 76% JPEG; Chromium size/dimension regression and four existing PDF export browser flows passed",
          "visualChecks": "1440x1000 and 900x800: A4 surface and all frames contained; no horizontal overflow",
          "schemaV7Compatibility": "小清新人像拍摄 persists schema v8 with 7 merged components, no numbered continuation names, and all 107 images; reference cards persist exact natural heights; page 1 contains 拍摄计划 + 延安西路 and page 2 contains 拍摄注意点 + 夏日西瓜; recovery manifests are retained"
        },
        "remaining": [
          "Split a single indivisible BlockNote block taller than one printable page at measured visual-line boundaries; current behavior fails closed with a contextual error",
          "Remove the retained v8-to-v6 PDF adapter after porting text measurement/drawing directly to document-flow placements"
        ]
      }
    },
    {
      "name": "Requirement 0809 Editor UX Enhancements",
      "description": "Extends the Editorial Studio workbench with recursive text layouts, contextual selected-text formatting, direct image pixel sizing, GUID-based project actions, and occupancy-based responsive A4 fitting.",
      "status": "completed",
      "feature_descriptions": [
        "Schema v10 stores each text component as a recursively splittable columns/rows tree of title-free rich-text leaves; v9 leaf titles are removed during migration",
        "Text cards render exactly two visible frame levels: the outer component and one or more leaf editors; recursive split parents are geometry-only",
        "Text leaves never scroll internally; columns align to the tallest natural sibling and width reflow grows or shrinks the whole card in both directions",
        "BlockNote formatting is contextual to an active selection and includes persistent 10/12/14/16/18/24px font sizing",
        "Reference group average image height supports controlled px input with validation, Escape restore, and existing 4pt minus/plus steps",
        "Project rail reveals full paths plus Open Directory and confirmed registry-only Remove actions without deleting files",
        "Duplicate project display titles are allowed; only colliding directory names receive a numeric suffix",
        "Insert aligns to the A4 left edge and automatic fitting keeps white paper near 82% of the full gray canvas width",
        "PDF export renders title-free recursive text leaves in matching split rectangles without title spacing",
        "Split text leaves keep Columns2/Rows2 controls followed by a rightmost delete control; deletion requires confirmation, promotes the sibling subtree, and supports visible or keyboard undo",
        "Text cards use compact 6pt visual insets and content-aware four-edge resize locks; every recursive leaf stays inside the frame with no scrollbar while natural height follows width reflow",
        "Formatting-toolbar pointer interactions preserve and restore the active ProseMirror selection so bold, italic, underline, strike, and font-size commands apply reliably in full-width and narrow split leaves"
      ],
      "decisions": [
        "Keep component and tree node IDs internal; component names remain display metadata while text leaves have no title field",
        "Migrate v8 text HTML deterministically into a single leaf named <componentId>:root, then migrate v9 trees to v10 by recursively removing titles without changing IDs or geometry",
        "Use a fixed 10pt gap for new splits, preserve legacy persisted gaps, and preserve the outer component bounds at split time",
        "Keep split controls separate from the BlockNote selection formatting toolbar",
        "Persist image geometry in points and convert UI values with 1pt = 4/3px",
        "Soft removal targets projectId and automatically opens the next recent available project",
        "Use a dedicated open_project_directory Tauri command rather than exposing filesystem behavior to React",
        "Calculate fit from scroller clientWidth so padding does not reduce the visible paper occupancy",
        "Measure the title-free full leaf/tree surface at its current width; split trees grow or shrink their component border instead of clipping or scrolling content",
        "Keep the Insert toolbar inside the canvas scroller so it shares exact centering and horizontal scroll geometry with the A4 paper",
        "Use a custom click-safe font-size menu because a native select cannot both open normally and preserve the BlockNote selection",
        "Keep BlockNote rather than migrate to Lexical; use plan-specific zero-height chrome, a 5pt frame padding plus 1pt border, a recursive 132pt leaf-width floor, and measured 64pt-or-natural height limits",
        "Clamp pointer previews before rendering, preserve the opposite edge for left/top resizes, and announce blocked movement with an amber edge plus an aria-live status"
      ],
      "progress": {
        "completedAt": "2026-08-10",
        "designSpec": "docs/superpowers/specs/2026-08-09-editor-ux-enhancements-design.md",
        "designReference": "docs/design_refs/preshot-requirement-0809-workbench.html",
        "twoLayerTextCardSpec": "docs/superpowers/specs/2026-08-10-two-layer-text-card-design.md",
        "twoLayerTextCardReference": "docs/design_refs/preshot-text-card-two-layer.html",
        "contentFitResizeSpec": "docs/superpowers/specs/2026-08-10-text-card-content-fit-resize-design.md",
        "lastVerified": {
          "unitTests": "83 files, 456 tests passed",
          "e2eTests": "42 Edge tests passed",
          "typecheck": "passed",
          "lint": "0 errors; existing ThemeProvider Fast Refresh warning remains",
          "rustTests": "48 passed",
          "build": "passed with existing Vite chunk-size warning",
          "followUpVisualAcceptance": "960x720: Insert toolbar/A4 left-edge delta 0px, component close icon renders at 10.3px under active scale, font-size menu opens by real click, text frame has no overflow, and page has no horizontal overflow",
          "twoLayerTextCardAcceptance": "Schema v1-v9 converges to strict title-free v10; recursive E2E verifies exactly two visible frame levels, 10pt new split gaps, equal-height column leaves, no internal scroll containers, narrow-grow/wide-shrink convergence, globally portaled delete confirmation, sibling promotion, visible Undo, Ctrl+Z restoration, persistence, and title-free PDF export",
          "contentFitResizeAcceptance": "Single-leaf body occupancy measured 97.96% x 92.05%; a two-column card stopped at 286pt with both leaves about 132.96pt, fully contained and scrollbar-free; horizontal preview height changed immediately with wrapping; amber aria-live feedback appeared at the limit; page horizontal overflow remained false; screen and PDF use matching compact plan geometry"
        },
        "remaining": []
      }
    },
    {
      "name": "PowerPoint-style Font Size and Color Controls",
      "description": "Redesigns the contextual rich-text toolbar with compact font-size and font-color split dropdowns, a theme palette, and progressive custom hue/RGB selection.",
      "status": "completed",
      "feature_descriptions": [
        "Font size moves immediately after strike and before paragraph alignment, displaying a compact numeric value plus a dedicated dropdown arrow without a visible px suffix",
        "Font color sits beside font size as an A split button with a current-color underline, quick reapply action, and a compact 10-color theme palette",
        "The palette exposes More Colors as a second-level panel with a hue wheel, exact integer RGB fields from 0 to 255, a read-only HEX preview, and apply/cancel actions",
        "All formatting interactions preserve the active ProseMirror selection and retain existing HTML persistence, undo/redo, autosave, reload, and PDF rendering",
        "Paragraph, font-size, font-color, and link surfaces use controlled WebView-safe popups that remain open through full pointer clicks",
        "All formatting dropdowns portal to the document body, escape the BlockNote toolbar clipping boundary, track their trigger while scrolling, and flip or shift to stay inside the viewport",
        "Every text leaf always displays its own formatting toolbar; narrow leaves retain core commands and expose alignment plus nesting through a responsive More portal without horizontal overflow",
        "Block menus use a readable 220px single-line layout, while the component close control remains a fixed 18px screen-space button in a reserved internal top-right slot at every canvas zoom"
      ],
      "decisions": [
        "Keep BlockNote and schema v10; persist font size and text color as editor-generated inline HTML styles",
        "Use the approved Editorial Studio palette and compact white toolbar rather than the generic purple marketing style returned by the design database",
        "Keep size presets at 10, 12, 14, 16, 18, and 24 and omit px from the collapsed control",
        "Use ten opaque theme colors, with color indicated by both accessible labels and visual swatches",
        "Normalize custom RGB colors to uppercase six-digit HEX before applying the BlockNote textColor style",
        "Do not support alpha transparency or direct HEX editing in this phase",
        "Use progressive disclosure so RGB controls appear only after More Colors is selected",
        "Prefer a maintained React color-wheel dependency after package-size and license review instead of hand-rolling wheel geometry"
      ],
      "progress": {
        "approvedAt": "2026-08-10",
        "completedAt": "2026-08-10",
        "designSpec": "docs/design_docs/formatting_toolbar_size_color_design.md",
        "designReference": "docs/design_refs/preshot-font-size-dropdown-review.html",
        "visualValidation": "1440x900 review: 473px toolbar, 146px theme palette, and 244x225px custom color panel remained within the paper and viewport with no horizontal or vertical overflow",
        "implementation": "Installed react-colorful 5.8.0; added strict RGB/HEX normalization utilities; implemented a persistent toolbar above every text leaf, responsive More access for low-frequency commands, compact size and color split controls with palette and custom RGB panel, selection preservation, target-level WebView-safe pointerdown activation, viewport-aware body portals, readable 220px block menus, fixed 18px component close chrome, runtime-only toolbar height measurement, HTML persistence, and PDF input coverage.",
        "lastVerified": "84 Vitest files / 461 tests and 47 Playwright tests passed; UI coverage verifies one persistent overflow-free toolbar per text leaf, readable block menus, fixed close-button geometry, pointerdown and full-click opening for all secondary surfaces, responsive More commands, bold, italic, underline, strike, alignment, nest/unnest, paragraph type, link, font-size, theme color and custom RGB mutations, autosave/reload persistence, and PDF parity; TypeScript and build passed; lint has 0 errors and the existing ThemeProvider Fast Refresh warning.",
        "remaining": []
      }
    },
    {
      "name": "TipTap Rich Text Editor Migration",
      "description": "Replaces BlockNote with TipTap 3 while preserving schema-v10 HTML, the persistent Preshot toolbar, runtime text sizing, autosave, and PDF parity.",
      "status": "completed",
      "feature_descriptions": [
        "TipTap remains behind the existing RichTextEditor props so domain models, recursive text leaves, project files, and PDF inputs do not change",
        "The current persistent toolbar, responsive More menu, 220px block menu, theme/custom colors, font sizes, links, and portal geometry remain visually consistent",
        "TipTap TextStyle, Color, FontSize, TextAlign, StarterKit Link and block commands emit HTML already understood by the current PDF parser",
        "Top-level block serialization moves from BlockNote lossy helpers to ProseMirror doc.content plus DOMSerializer",
        "Top-level TipTap DOM children provide editor-neutral block measurement boundaries"
      ],
      "decisions": [
        "Target TipTap 3.29.2 and use only MIT open-source editor packages",
        "Keep schema v10 and HTML persistence; do not bulk-rewrite existing project files",
        "Keep the RichTextEditor public adapter stable and remove BlockNote only after focused and full validation pass",
        "Use official FontSize from @tiptap/extension-text-style rather than restoring the previous custom extension",
        "Treat collapsible headings as an explicit custom-node follow-up instead of claiming StarterKit supports them"
      ],
      "progress": {
        "designedAt": "2026-08-10",
        "designSpec": "docs/design_docs/tiptap_migration_design.md",
        "interactivePoc": "docs/design_refs/preshot-tiptap-editor-demo.html",
        "splitInteractivePoc": "docs/design_refs/preshot-tiptap-split-editor-demo.html",
        "unifiedCardClosePoc": "docs/design_refs/preshot-unified-card-close-demo.html",
        "pocValidation": "Actual TipTap 3.29.2 loaded; color wrote rgb(8, 145, 178), font size wrote 24px, HTML persisted across reload, the 220px block menu kept 36px single-line rows, More commands executed, and 920x720 had no page or toolbar overflow.",
        "splitPocValidation": "Actual independent TipTap instances rendered in recursive columns/rows nodes; mixed nesting grew from three to four leaves, formatting stayed isolated per leaf, removing a nested leaf collapsed its sibling into place, narrow toolbars exposed hover-only left/right scroll arrows, and per-leaf color controls preserve the original ProseMirror selection across portal interactions while supporting theme colors plus a circular HSV wheel, brightness, strict RGB input, and verified TipTap HTML output.",
        "unifiedCardClosePocValidation": "The text and reference cards share one 16pt graphite close button, 5pt top-right inset, 20pt content gutter, 4px radius and danger hover state. Browser measurements matched at full size and at 58% card width / 70% canvas zoom; both buttons stayed inside without content overlap. The 390px layout had no horizontal overflow. The approved design is implemented in the production shared ComponentFrame.",
        "midsceneAutomationReport": "midscene_run/report/E2E-Preshot-Plan-Text-Final-2026-08-11T09-25-39-015Z.html",
        "midsceneAutomationValidation": "Eight gpt-5.6-sol Midscene journeys passed using isolated fresh projects: lifecycle, block/inline formatting, colors and links, list/quote/code, recursive splits, reorder/resize chrome, save/reload/undo, and PDF/delete cleanup. Every formal run removed its project through the UI and reported zero Midscene-prefixed storage residue. M07 found that newly inserted runtime components leaked y into persisted v10 data; UI constructors now omit y and addComponent explicitly projects schema fields before persistence.",
        "implementation": "TipTap 3.29.2 now powers every production RichTextEditor leaf with StarterKit, TextStyle, Color, FontSize, TextAlign, TableKit, TaskList and TaskItem. Plan and reference cards render the same shared scale-aware ComponentCloseButton: 16pt size, 5pt internal top-right anchor, graphite default, danger hover/focus, matching icon geometry and a 20pt content gutter. Plan minimum width includes the gutter; reference names shrink before it, and both controls stay contained throughout live resize previews. Clicking non-interactive card whitespace or transparent resize chrome now collapses a rich-text selection to its original block end; editable content and controls retain native behavior so formatting commands still preserve the selected range. The persistent toolbar retains viewport-safe portals, selection restoration, 220px block menus, responsive More commands and adds hover-only horizontal scroll arrows for narrow leaves. More formatting contains only six non-duplicate commands; left, center and right alignment remain exclusively in the persistent toolbar. Every persistent-toolbar and More-command button exposes a Chinese native hover title matching its accessible function name. H1-H6 restore explicit 32/24/20/18/16/14px visual hierarchy with 700 weight, and heading conversion removes a prior explicit font-size mark so it cannot hide the selected heading level. The size control reports the selected text's effective visual size from inline marks or its paragraph/heading level, offers every heading size, and formatting portals close on outside pointer input. Ordinary bullet and ordered lists explicitly restore disc and decimal markers removed by Tailwind preflight while task lists remain marker-free. TipTap em marks locally enable synthesized italic rendering because the bundled Noto Sans SC faces are upright-only and the application otherwise disables font synthesis. Custom colors use a circular HSV wheel whose right-origin hue and full-radius saturation gradient match its coordinate algorithm, plus brightness and strict RGB input. Portal pointer events are excluded from toolbar selection capture, preserving the original text range until a custom color is applied and the selection is collapsed. ProseMirror DOMSerializer emits top-level blocks for pagination measurement. BlockNote, Mantine editor styles and react-colorful were removed.",
        "lastVerified": "Eight isolated Midscene plan-text journeys, 86 Vitest files / 468 tests, and 54 Playwright tests passed; TypeScript and production build passed; lint has 0 errors and the existing ThemeProvider Fast Refresh warning. AI and deterministic coverage verifies fresh-project lifecycle and cleanup, strict v10 save/reload after insertion and recursive split, identical plan/reference close chrome, selection behavior, toolbar/popup commands, headings, lists, nesting, quote/code, theme/wheel/RGB colors, link/unlink, persistence, undo/redo, PDF export and deletion.",
        "remaining": []
      }
    },
    {
      "name": "Inline Rich Text Images",
      "description": "Allows photography-plan text leaves to insert project-managed images as block content while preserving compact HTML, reload behavior, responsive canvas layout, and PDF output.",
      "status": "completed",
      "feature_descriptions": [
        "Every plan text leaf exposes an Insert Image button in the persistent TipTap toolbar with a Lucide image icon, Chinese accessible label, native hover title, and import-in-progress state",
        "Inserted images render as proportional block content capped to the current text leaf width and expose a visible ProseMirror node-selection outline",
        "Hovering or selecting an inserted image exposes a bottom-right drag handle; resizing preserves its aspect ratio, stays within the text leaf, and persists width and height for reload and PDF export",
        "Project reload resolves persisted relative image paths to runtime data URLs without writing base64 image data into the plan manifest",
        "PDF text parsing, natural-height measurement, pagination, and drawing treat each inserted image as a whole proportional content block in both ordinary and recursively split text leaves",
        "A standalone interactive demo includes a complete portrait-shooting sample and supports inserting a local JPG, PNG, or WebP at the current editing position"
      ],
      "decisions": [
        "Use the official @tiptap/extension-image package pinned to TipTap 3.29.2 and extend its attributes only for project asset identity and source dimensions",
        "Copy text images into the existing references directory through a non-mutating CanvasPlanService.importAsset boundary; do not add them to a reference-image group",
        "Persist only references-relative src values in TipTap HTML and resolve them to data URLs at the editor boundary",
        "Keep inserted images as block nodes and resize them proportionally from one bottom-right handle; captions, alignment, and crop controls remain separate follow-ups",
        "Reuse the PDF export image map and optimizer rather than introducing a second filesystem path into the exporter"
      ],
      "progress": {
        "completedAt": "2026-08-11",
        "interactivePoc": "docs/design_refs/preshot-inline-rich-text-image-demo.html",
        "pocValidation": "Desktop and 375px browser checks rendered the sample image without page overflow; the compact toolbar scrolls horizontally at mobile width. Selecting city-02.png through the real file chooser increased the editable image count from one to two and preserved the file name as alt text. Dragging the sample image handle changed it from 644x966 to 524x786, preserved its ratio, produced no page overflow, and changed the document state to unsaved.",
        "implementation": "Added a project-aware TipTap image extension and Insert Image toolbar command; introduced a non-mutating project asset import service; loaded plan-text image files alongside reference images; serialized editor data URLs back to relative references paths; added a pointer-driven proportional resize node view with a bottom-right handle and persisted dimensions; added responsive editor image styling; extended HTML-to-PDF blocks, rich-text height and page layout, bitmap embedding, split-leaf drawing, and cache reuse for rich-text images.",
        "lastVerified": "87 Vitest files / 482 tests, 55 Playwright tests, and 48 Rust tests passed; TypeScript and production build passed; lint has 0 errors and the existing ThemeProvider Fast Refresh warning. Focused coverage verifies non-mutating asset import, data-URL-free HTML persistence, proportional drag resizing, width/height reload persistence, whole-block pagination, and PDF bitmap embedding. Browser validation resized a project image from 1x1 to 91x91 and reloaded the same dimensions from relative-path HTML.",
        "remaining": []
      }
    },
    {
      "name": "Reference Image View WYSIWYG Export",
      "description": "Persists each image's visible crop separately from its immutable source so canvas, PDF, and PPT render the same composition.",
      "status": "in-progress",
      "feature_descriptions": [
        "Source images retain their original file and pixel dimensions while a view stores frame dimensions plus a normalized crop rectangle",
        "Dragging view edges changes the frame ratio, dragging inside adjustment mode pans the crop, and zoom changes crop size without modifying the source",
        "Canvas, PDF, and PPT consume one domain-level ImageViewRenderSpec; export adapters cannot independently contain, cover, or recenter images",
        "Double-click continues to open the complete source image and Reset restores source ratio plus a full-image crop",
        "Schema migration derives a centered cover crop so existing object-cover projects retain their current canvas appearance"
      ],
      "decisions": [
        "Persist crop as normalized source coordinates x/y/width/height so it is independent of canvas zoom, DPR, PDF DPI, and PPT units",
        "Replace the current PDF referenceImageDrawBox contain behavior with the shared view render pipeline",
        "Render deterministic view bitmaps for PDF and PPT initially; direct PDF crop and OOXML srcRect optimizations require pixel-equivalence tests",
        "Keep image reorder separate from crop adjustment by using an explicit adjust-view interaction mode"
      ],
      "progress": {
        "designedAt": "2026-08-11",
        "designSpec": "docs/design_docs/reference_image_view_ppt_design.md",
        "interactivePoc": "docs/design_refs/preshot-image-view-ppt-wysiwyg-demo.html",
        "pocValidation": "A local 2000x1200 source was rendered into arbitrary 4:5, panned, and 16:9 views. Browser checks confirmed canvas, PDF, and PPT image width/height/left/top crop styles were identical in every state with no horizontal overflow. The legacy contain toggle visibly made both exports diverge.",
        "projectMigrationValidation": "Migrated the real 小清新人像拍摄 manifest from a transitional plan marked v10 but already containing v11 crop/source fields. The migration created a timestamped backup, strictly validated schema v11, preserved 6 components and 106 image references with zero missing files, was idempotent on a second dry-run, and passed a read-only desktop visual check in the latest Tauri app.",
        "implementation": "Implemented schema-v11 optional source dimensions and normalized crop with strict validation plus centered-cover v10 migration. A shared ImageViewRenderSpec drives explicit canvas CSS and crop-aware PDF bitmap generation. Per-image edge resize preserves crop focus, adjustment mode pans without conflicting with reorder, autosave/reload preserves the view, reset restores source ratio/full crop, and the lightbox still shows the immutable source. PDF caching is keyed by image view identity rather than source file. No PPT adapter exists yet.",
        "remaining": [
          "Add an explicit crop zoom control and persistence coverage",
          "Add PPTX adapter and rendered-output pixel comparison tests"
        ],
        "lastVerified": "Full validation passed: 87 Vitest files / 475 tests, 55 Playwright tests, 48 Rust tests, TypeScript, lint with 0 errors and the existing ThemeProvider warning, and the production build with the existing large-chunk warning. Image-view coverage includes strict v11 migration, frame-ratio resize, explicit crop rendering, adjustment-mode pan, autosave/reload persistence, immutable-source lightbox, full-source reset, crop-aware PDF bitmap generation, and draw-box-ratio JPEG optimization."
      }
    },
    {
      "name": "Continuous Document Canvas v12",
      "description": "Replaces separate text and reference cards with one unrestricted TipTap document containing resizable atomic image-group nodes.",
      "status": "completed",
      "feature_descriptions": [
        "All prose before, between, and after image groups belongs to one continuous TipTap editor and can extend to the end of the document",
        "Image groups default to full width and remain atomic document nodes; group frame, image frame, crop, source, caption, and ordering metadata remain project-managed",
        "Images and image groups expose four edge and four corner resize handles constrained to the printable text boundary",
        "The contextual image-group toolbar is a compact 30px surface aligned to the selected group's upper-right corner and contains no size controls",
        "Images reorder within and across image groups with a stable DOM and live insertion placeholder",
        "Clicking an image opens the full-size viewer; hover deletion requires confirmation and no reset-size action exists",
        "Dragging a selected image group's gray background reorders the whole atomic node among text blocks and other image groups",
        "The top Insert control plus blank-line and page-end plus controls expose only Image Group insertion",
        "Schema v1-v11 projects migrate to strict v12 while preserving visual text order and reference-image metadata",
        "Canvas and PDF consume the same document marker order, including crop-aware image rendering"
      ],
      "decisions": [
        "Make documentHtml the sole canonical source for text content and image-group order; retain components only for image-group metadata",
        "Represent each image group with a stable data-preshot-node=image-group marker and require every group record and marker to match exactly once",
        "Keep the old component canvas only as a compatibility rendering branch for in-memory fixtures without documentHtml; never persist dual v11/v12 canonical stores",
        "Render contextual toolbars through a document.body portal so A4 scaling cannot change their screen-space size or positioning",
        "Derive the temporary PDF layout from canonical v12 documentHtml rather than reintroducing editable text components",
        "Migrate manifests through timestamped backups, temporary-file atomic replacement, and strict schema-v12 post-write verification",
        "Render Word-style page corner marks outside the printable text rectangle, with inward-pointing vertices exactly defining its four corners",
        "Persist group resize through existing x/width/height fields and image resize through frameWidth/frameHeight plus the single normalized crop; use a transient shared fit scale only when the resized group cannot contain its images",
        "Use pointer activation thresholds, same-node drag previews, and explicit placeholders for image and image-group movement so click, resize, and drag gestures remain distinct",
        "Remove whole-group image scaling controls and image reset commands from the document-mode UI; manual frame handles are the only size-changing interaction",
        "Expose image resize directly on hover with balanced 20px edge and 24px corner zones fully inside the image frame, while keeping image controls above group resize chrome",
        "Persist signed image frame offsets and a signed group vertical offset so top/left resizing preserves the opposite edge across reload and PDF export",
        "Use a body-level group resize preview and commit geometry only on pointerup to avoid ResizeObserver, image reflow, and pagination flicker",
        "Replace the legacy yellow image snap lines with semantic Smart Guides: magenta dashed edge/center guides, dimension brackets, explicit labels, frozen candidate geometry, one position guide per axis, and 6px/10px hysteresis"
      ],
      "progress": {
        "designedAt": "2026-08-12",
        "completedAt": "2026-08-12",
        "designSpec": "docs/design_docs/canvas_design.md",
        "interactivePoc": "docs/design_refs/preshot-canvas-document-flow-demo.html",
        "pageCornerReview": "docs/design_refs/preshot-inward-page-corners-review.html",
        "pictureCardReview": "docs/design_refs/preshot-picture-card-review.html",
        "approvedPictureCardRevision": "docs/design_refs/preshot-picture-card-resize-v2.html",
        "approvedAt": "2026-08-13",
        "interactionRevisionCompletedAt": "2026-08-13",
        "resizeRefinementCompletedAt": "2026-08-14",
        "smartGuidesCompletedAt": "2026-08-14",
        "smartGuidesReview": "docs/design_refs/preshot-image-smart-guides-review.html",
        "smartGuidesPlacementFix": "Equal-width labels now center from their measured size with translateX(-50%) and a fixed gap below the bracket; equal-height labels sit to the bracket's right with translateY(-50%).",
        "pictureCardPrototypeComparison": "Production now matches the approved resize-v2 HTML: image and group four-edge/four-corner transparent resize zones, non-text grab/default cursors, 30px upper-right toolbars without size controls, no reset action, single-click full-size viewing, hover delete with confirmation, stable same-node image resize/drag, within-group and cross-group image movement, and gray-surface whole-group document reordering among prose and other groups.",
        "implementation": "Kept the strict v12 TipTap document architecture and upgraded its image-group NodeView interactions. Image and group resize continue to persist frame/crop and x/width/height metadata while reusing existing frame/button/img nodes. The contextual image and group toolbars are compact 30px upper-right portals; the group bar contains only type, add, and delete actions. Removed image reset UI and its domain command. Single-click image activation opens the full-size viewer while retaining NodeSelection; drag activation uses a movement threshold so image clicks do not become drags. Images move within and across groups through the existing moveImage reducer, with the original frame DOM temporarily lifted to a body-level drag preview and explicit target placeholders so source decoding and identity remain stable. Hover delete and toolbar delete share a globally portaled confirmation dialog. Pressing and dragging a group's gray background applies a non-layout-shifting preview plus a document drop indicator, then commits one ProseMirror transaction that moves the atomic marker among text blocks and other groups. Image resize Smart Guides now freeze candidate geometry at pointerdown, use 6px entry and 10px release hysteresis, prioritize equal dimensions over edges and centers, render one magenta dashed position guide per axis with endpoint markers and semantic labels, and separately render dimension brackets plus `同宽/同高` labels. Preview writes are requestAnimationFrame-batched and all overlays clear on finish or cancel. Existing pagination, PDF marker order, autosave, undo/redo, migration, and crop-aware rendering remain unchanged.",
        "projectMigrationValidation": "Wrote and strictly verified schema v12 for 小清新人像拍摄 (v11 to v12, 5 image groups, 106 images) and Sunset Shanghai (v8 to v12, 3 image groups, 17 images). Both writes created timestamped pre-schema-v12 backups.",
        "lastVerified": {
          "date": "2026-08-13",
          "typecheck": "clean",
          "lint": "0 errors; existing ThemeProvider Fast Refresh warning remains",
          "unitTests": "88 files, 497 tests passed",
          "e2eTests": "29 Playwright journeys passed. Canvas coverage includes semantic Smart Guides for true edge alignment and equal dimensions, dashed rendering and cleanup, pointer cursors on image/button/img, hover-active balanced resize zones, anchored image top-edge resize, stable body-level group resize preview, upward group-top resize with fixed lower edge, eight-way resize, compact toolbar placement, click-to-view, confirmed hover deletion, stable within/cross-group image drag, gray-surface group/document reordering, persistence, pagination, and PDF export",
          "rustTests": "48 passed",
          "build": "passed with the existing Vite large-chunk warning"
        },
        "visualVerification": "Playwright exercised the running browser shell with real pointer input: equal-width resizing displayed only a dimension bracket and `同宽` label, while a true edge match displayed a magenta dashed guide and `右边对齐` label; all overlays disappeared after pointerup. Unselected images exposed balanced inner resize zones with a pointer cursor; top-edge shrinking preserved the bottom and DOM identity; group top-edge dragging left the live group and images unchanged while a body-level preview moved upward, then committed once with the lower edge fixed. Existing image drag, group drag, deletion confirmation, lightbox, persistence, pagination, and PDF flows also passed.",
        "remaining": [
          "Open both migrated projects in an interactive Windows desktop session and visually confirm image-group counts, crop/frame rendering, contextual controls, and PDF export"
        ]
      }
    },
    {
      "name": "BlockNote Canvas v14 Multi-Column",
      "description": "Provides a native BlockNote JSON canvas, React image-group custom block, and GPL-licensed multi-column rows while migrating schema v13 and blocking older schemas.",
      "status": "completed",
      "feature_descriptions": [
        "New projects persist strict schema-v14/document-v2 BlockNote-style JSON instead of documentHtml; schema v13 migrates automatically",
        "Native BlockNote Mantine formatting, slash menu, side menu, block drag, tables, lists, code, and undo/redo drive the document canvas",
        "Image groups are content-none custom blocks containing only groupId; they may be top-level or direct column children while detailed metadata remains separate",
        "Image-group internal import, view, deletion, drag, eight-way resize, crop layout, and PDF behavior are retained",
        "Imported images are copied into the project references directory with new names while their original source files remain untouched",
        "Same-project duplication creates new group/image IDs while reusing image files",
        "Schema v1-v12 projects are rejected without opening, exporting, autosaving, or modifying files",
        "The editing canvas is one continuous white BlockNote document; A4 pagination is deferred to PDF export",
        "PDF export traverses JSON directly without HTML round-tripping",
        "Text and image-group blocks can share a row using resizable columnList/column blocks",
        "Native BlockNote image, video, and audio blocks support project-managed uploads, reload, block operations, columns, and export fallbacks"
      ],
      "decisions": [
        "Use published BlockNote packages and Mantine UI; local C:\\projects\\BlockNote is source reference only",
        "Use BlockNote native text features and accept H4-H6/font-size downgrade",
        "Image-group blocks remain top-level except when used as direct column children",
        "Internal image interactions use bn-drag-exclude",
        "Keep runtime tombstones for native delete/undo and purge files at project retirement",
        "BlockNote v14 is the sole editable canvas; the legacy TipTap provider, extensions, tests, and direct dependencies were removed"
      ],
      "progress": {
        "designedAt": "2026-08-14",
        "implementationPlan": "session plan.md",
        "designSpec": "docs/design_docs/blocknote_v13_migration_design.md",
        "viewportDesignSpec": "docs/design_refs/preshot-canvas-focus-zoom-demo.html",
        "blockOperationsDesignSpec": "docs/design_docs/block_operations_design.md",
        "multiColumnResearchSpec": "docs/design_docs/blocknote_multi_column_research.md",
        "designed": [
          "Proposed a fixed 1080px logical document width with 20px viewport gutters and only 24px after the document end",
          "Revised the viewport proposal after review: project and assistant panels stay visible by default, while focus mode remains optional",
          "Proposed opening at fit-width zoom and accelerating pointer-anchored Ctrl+wheel zoom to 15% per step",
          "Designed a complete BlockNote Side Menu control system for insert, move, subtree duplication, transform, delete/undo, nest, unnest, keyboard shortcuts, and drag/drop feedback",
          "Initially kept imageGroup top-level, then revised the rule to allow direct column children while retaining other nesting restrictions",
          "Researched official BlockNote multi-column support: text and custom imageGroup blocks can share a row through columnList/column from @blocknote/xl-multi-column",
          "Identified required schema 14 persistence, image-group column allowance, pointer left/right drops, column-aware operations, PDF layout, and GPL-3.0 or proprietary license decision"
        ],
        "implemented": [
          "Added strict portable v14 block JSON validation and new-project seeds",
          "Added typed legacy incompatibility load state",
          "Installed BlockNote/Mantine and added restricted schema plus imageGroup React block",
          "Installed @blocknote/xl-multi-column 0.53.0 under GPL-3.0 with full license and third-party notice",
          "Upgraded persistence to schema 14/document version 2 with automatic schema 13 migration",
          "Added two-column and three-column slash items, resizable columns, and left/right pointer edge drops",
          "Allowed image groups at the top level or directly inside columns and made metadata reference traversal recursive",
          "Added column-aware PDF layout with persisted weight ratios and keep-together row pagination",
          "Enabled native BlockNote image, video, and audio block specs and uploadFile integration",
          "Added project media storage under media/ with validated MIME types, size limits, data-URL runtime resolution, relative-path JSON persistence, undo-safe retirement cleanup, browser fixtures, and narrow Rust commands",
          "Added native image PDF rendering plus explicit video/audio caption or link fallbacks and Tauri media-src CSP",
          "Added JSON autosave, Chinese Mantine UI, slash insertion and custom side-menu duplication",
          "Replaced runtime page surfaces with one continuous white canvas that grows with BlockNote content",
          "Expanded the continuous document to a fixed 1080px logical width with 36px page padding, about 1008px of editable content, 20px workspace gutters, and a viewport-filling minimum height",
          "Kept project and assistant panels visible by default, retained optional focus overlays, and maximized the native Windows application window whenever a project opens",
          "Changed initial zoom to automatic fit-width and accelerated pointer-anchored Ctrl+wheel zoom to 15% per step across a 55%-180% range",
          "Added a complete custom BlockNote drag-handle menu with block metadata, quick move/outdent/indent controls, relative insertion, subtree duplication, type conversion, delete/undo, disabled reasons, and nested structure guides",
          "Added same-level subtree movement so menu arrows never change nesting implicitly",
          "Replaced unreliable BlockNote HTML5 block dragging under CSS zoom with a custom Pointer Events drag handle, 6px activation threshold, before/after/inside targets, fixed drop overlays, full-subtree transactions, and image-group hierarchy enforcement",
          "Allowed image-group blocks to start the same Pointer block drag from their gray empty surface while excluding images, buttons, and resize handles",
          "Added Ctrl+D duplication and Alt+Arrow block movement shortcuts; Tab and Shift+Tab retain native BlockNote nesting behavior",
          "Constrained image-group cards to the actual BlockNote content width with exact left/right alignment",
          "Added pointer-anchored Ctrl+wheel continuous-canvas zoom from 50% to 150%",
          "Added image import/view/delete, image/group resize, image reorder and basic Smart Guides",
          "Removed the blue group-resize overlay; the image-group card now follows the pointer directly during resize",
          "Changed native image import from move/rename semantics to project-local copy semantics so original source images remain untouched",
          "Added duplicate reconciliation, top-level enforcement and runtime tombstones",
          "Added direct JSON PDF mapping and exporter",
          "Added independent BlockNote Playwright configuration",
          "Fixed Enter/new-line creation by removing pre-change block diff inspection before BlockNote assigns the new block ID; top-level image-group normalization now runs after changes",
          "Measured imported source dimensions and laid out batch images at equal height with width derived from each original aspect ratio"
        ],
        "dependencyNote": "Organization npm mirror provides BlockNote core/react/mantine and @blocknote/xl-multi-column at 0.53.0. The XL package is used under GPL-3.0; exact 0.54.x upgrade remains a follow-up.",
        "lastVerified": {
          "date": "2026-08-17",
          "lint": "0 errors; existing ThemeProvider Fast Refresh warning remains",
          "typecheck": "passed",
          "unitTests": "88 files, 458 tests passed",
          "e2eTests": "15 unified BlockNote/workspace/layout/theme/PDF Playwright journeys passed",
          "focusedBlockNoteE2E": "8 BlockNote v14 journeys passed, including v13 migration, nested block controls, pointer dragging, columns, image groups, and native image/video/audio upload, relative-path persistence, reload, and PDF export",
          "initializer": "4 PowerShell initializer checks passed",
          "rustTests": "50 passed",
          "build": "pnpm build passed with no direct TipTap dependencies and the existing large-chunk warning",
          "tauriBuild": "passed with multi-column and native media commands/CSP; produced Preshot_0.1.0_x64_en-US.msi"
        },
        "visualVerification": "At a 1440px viewport both side panels are visible by default and the fixed 1080px document fits the remaining center workspace at 85%. Multi-column text/image-group rows align within 0.02px, gray-surface image-group dragging creates one group per column, and the XL divider persists width weights. Native BlockNote image, video, and audio blocks rendered their preview/player controls and captions from project-managed media; reload restored them from relative media paths. PDF renders native images and explicit video/audio fallback rows.",
        "remaining": [
          "Upgrade BlockNote packages to 0.54.x when available",
          "Complete Midscene visual validation for standard BlockNote block dragging"
        ]
      }
    }
  ]
}
